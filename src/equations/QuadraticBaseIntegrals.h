#pragma once
#include "BaseIntegrals.h"

/************************************************************************/
/*
    Structure of the base integrals are as follows:
        [shape functions (27)] [derivative x,y,z (3)] [test function x,y,z (3)], [cells (8)]

*/
/************************************************************************/

class QuadraticBaseIntegrals : public BaseIntegrals {
public:

    QuadraticBaseIntegrals(ettention::Vec3f& voxelSize) :
        BaseIntegrals(voxelSize)
    {
        init();
    }

    QuadraticBaseIntegrals(ettention::Vec3d& voxelSize) :
        BaseIntegrals(voxelSize)
    {
        init();
    }

    //From BaseIntegrals
    //  inline REAL* data()
    //  inline REAL value(unsigned int basisFunc, unsigned int deriv, unsigned int testFunc, unsigned int cell)

protected:
    //From BaseIntegrals:
    //  REAL dx;
    //  REAL dy;
    //  REAL dz;
    //  REAL values[27 * 3 * 3 * 8];

    void init() override {
        MAP(values,0,0,0,0) = static_cast<REAL>(-3703) / 86400 / dx * dy * dz;
        MAP(values,0,0,0,1) = static_cast<REAL>(-529) / 86400 / dx * dy * dz;
        MAP(values,0,0,0,2) = static_cast<REAL>(1127) / 86400 / dx * dy * dz;
        MAP(values,0,0,0,3) = static_cast<REAL>(161)/ 86400 / dx * dy * dz;
        MAP(values,0,0,0,4) = static_cast<REAL>(1127) / 86400 / dx * dy * dz;
        MAP(values,0,0,0,5) = static_cast<REAL>(161)/ 86400 / dx * dy * dz;
        MAP(values,0,0,0,6) = static_cast<REAL>(-343) / 86400 / dx * dy * dz;
        MAP(values,0,0,0,7) = static_cast<REAL>(-49) / 86400 / dx * dy * dz;
        MAP(values,0,0,1,0) = static_cast<REAL>(-1127) / 17280 * dz;
        MAP(values,0,0,1,1) = static_cast<REAL>(-161) / 17280 * dz;
        MAP(values,0,0,1,2) = static_cast<REAL>(-161) / 17280 * dz;
        MAP(values,0,0,1,3) = static_cast<REAL>(-23) / 17280 * dz;
        MAP(values,0,0,1,4) = static_cast<REAL>(343) / 17280 * dz;
        MAP(values,0,0,1,5) = static_cast<REAL>(49) / 17280 * dz;
        MAP(values,0,0,1,6) = static_cast<REAL>(49) / 17280 * dz;
        MAP(values,0,0,1,7) = static_cast<REAL>(7) / 17280 * dz;
        MAP(values,0,0,2,0) = static_cast<REAL>(-1127) / 17280 * dy;
        MAP(values,0,0,2,1) = static_cast<REAL>(-161) / 17280 * dy;
        MAP(values,0,0,2,2) = static_cast<REAL>(343) / 17280 * dy;
        MAP(values,0,0,2,3) = static_cast<REAL>(49) / 17280 * dy;
        MAP(values,0,0,2,4) = static_cast<REAL>(-161) / 17280 * dy;
        MAP(values,0,0,2,5) = static_cast<REAL>(-23) / 17280 * dy;
        MAP(values,0,0,2,6) = static_cast<REAL>(49) / 17280 * dy;
        MAP(values,0,0,2,7) = static_cast<REAL>(7) / 17280 * dy;
        MAP(values,0,1,0,0) = static_cast<REAL>(-1127) / 17280 * dz;
        MAP(values,0,1,0,1) = static_cast<REAL>(-161) / 17280 * dz;
        MAP(values,0,1,0,2) = static_cast<REAL>(-161) / 17280 * dz;
        MAP(values,0,1,0,3) = static_cast<REAL>(-23) / 17280 * dz;
        MAP(values,0,1,0,4) = static_cast<REAL>(343) / 17280 * dz;
        MAP(values,0,1,0,5) = static_cast<REAL>(49) / 17280 * dz;
        MAP(values,0,1,0,6) = static_cast<REAL>(49) / 17280 * dz;
        MAP(values,0,1,0,7) = static_cast<REAL>(7) / 17280 * dz;
        MAP(values,0,1,1,0) = static_cast<REAL>(-3703) / 86400 * dx / dy * dz;
        MAP(values,0,1,1,1) = static_cast<REAL>(1127) / 86400 * dx / dy * dz;
        MAP(values,0,1,1,2) = static_cast<REAL>(-529) / 86400 * dx / dy * dz;
        MAP(values,0,1,1,3) = static_cast<REAL>(161) / 86400 * dx / dy * dz;
        MAP(values,0,1,1,4) = static_cast<REAL>(1127) / 86400 * dx / dy * dz;
        MAP(values,0,1,1,5) = static_cast<REAL>(-343) / 86400 * dx / dy * dz;
        MAP(values,0,1,1,6) = static_cast<REAL>(161) / 86400 * dx / dy * dz;
        MAP(values,0,1,1,7) = static_cast<REAL>(-49) / 86400 * dx / dy * dz;
        MAP(values,0,1,2,0) = static_cast<REAL>(-1127) / 17280 * dx;
        MAP(values,0,1,2,1) = static_cast<REAL>(343) / 17280 * dx;
        MAP(values,0,1,2,2) = static_cast<REAL>(-161) / 17280 * dx;
        MAP(values,0,1,2,3) = static_cast<REAL>(49) / 17280 * dx;
        MAP(values,0,1,2,4) = static_cast<REAL>(-161) / 17280 * dx;
        MAP(values,0,1,2,5) = static_cast<REAL>(49) / 17280 * dx;
        MAP(values,0,1,2,6) = static_cast<REAL>(-23) / 17280 * dx;
        MAP(values,0,1,2,7) = static_cast<REAL>(7) / 17280 * dx;
        MAP(values,0,2,0,0) = static_cast<REAL>(-1127) / 17280 * dy;
        MAP(values,0,2,0,1) = static_cast<REAL>(-161) / 17280 * dy;
        MAP(values,0,2,0,2) = static_cast<REAL>(343) / 17280 * dy;
        MAP(values,0,2,0,3) = static_cast<REAL>(49) / 17280 * dy;
        MAP(values,0,2,0,4) = static_cast<REAL>(-161) / 17280 * dy;
        MAP(values,0,2,0,5) = static_cast<REAL>(-23) / 17280 * dy;
        MAP(values,0,2,0,6) = static_cast<REAL>(49) / 17280 * dy;
        MAP(values,0,2,0,7) = static_cast<REAL>(7) / 17280 * dy;
        MAP(values,0,2,1,0) = static_cast<REAL>(-1127) / 17280 * dx;
        MAP(values,0,2,1,1) = static_cast<REAL>(343) / 17280 * dx;
        MAP(values,0,2,1,2) = static_cast<REAL>(-161) / 17280 * dx;
        MAP(values,0,2,1,3) = static_cast<REAL>(49) / 17280 * dx;
        MAP(values,0,2,1,4) = static_cast<REAL>(-161) / 17280 * dx;
        MAP(values,0,2,1,5) = static_cast<REAL>(49) / 17280 * dx;
        MAP(values,0,2,1,6) = static_cast<REAL>(-23) / 17280 * dx;
        MAP(values,0,2,1,7) = static_cast<REAL>(7) / 17280 * dx;
        MAP(values,0,2,2,0) = static_cast<REAL>(-3703) / 86400 * dx * dy / dz;
        MAP(values,0,2,2,1) = static_cast<REAL>(1127) / 86400 * dx * dy / dz;
        MAP(values,0,2,2,2) = static_cast<REAL>(1127) / 86400 * dx * dy / dz;
        MAP(values,0,2,2,3) = static_cast<REAL>(-343) / 86400 * dx * dy / dz;
        MAP(values,0,2,2,4) = static_cast<REAL>(-529) / 86400 * dx * dy / dz;
        MAP(values,0,2,2,5) = static_cast<REAL>(161) / 86400 * dx * dy / dz;
        MAP(values,0,2,2,6) = static_cast<REAL>(161) / 86400 * dx * dy / dz;
        MAP(values,0,2,2,7) = static_cast<REAL>(-49) / 86400 * dx * dy / dz;
        MAP(values,1,0,0,0) = static_cast<REAL>(529) / 10800 / dx * dy * dz;
        MAP(values,1,0,0,1) = static_cast<REAL>(529) / 10800 / dx * dy * dz;
        MAP(values,1,0,0,2) = static_cast<REAL>(-161) / 10800 / dx * dy * dz;
        MAP(values,1,0,0,3) = static_cast<REAL>(-161) / 10800 / dx * dy * dz;
        MAP(values,1,0,0,4) = static_cast<REAL>(-161) / 10800 / dx * dy * dz;
        MAP(values,1,0,0,5) = static_cast<REAL>(-161) / 10800 / dx * dy * dz;
        MAP(values,1,0,0,6) = static_cast<REAL>(49) / 10800 / dx * dy * dz;
        MAP(values,1,0,0,7) = static_cast<REAL>(49) / 10800 / dx * dy * dz;
        MAP(values,1,0,1,0) = static_cast<REAL>(161) / 2880 * dz;
        MAP(values,1,0,1,1) = static_cast<REAL>(-161) / 2880 * dz;
        MAP(values,1,0,1,2) = static_cast<REAL>(23) / 2880 * dz;
        MAP(values,1,0,1,3) = static_cast<REAL>(-23) / 2880 * dz;
        MAP(values,1,0,1,4) = static_cast<REAL>(-49) / 2880 * dz;
        MAP(values,1,0,1,5) = static_cast<REAL>(49) / 2880 * dz;
        MAP(values,1,0,1,6) = static_cast<REAL>(-7) / 2880 * dz;
        MAP(values,1,0,1,7) = static_cast<REAL>(7) / 2880 * dz;
        MAP(values,1,0,2,0) = static_cast<REAL>(161) / 2880 * dy;
        MAP(values,1,0,2,1) = static_cast<REAL>(-161) / 2880 * dy;
        MAP(values,1,0,2,2) = static_cast<REAL>(-49) / 2880 * dy;
        MAP(values,1,0,2,3) = static_cast<REAL>(49) / 2880 * dy;
        MAP(values,1,0,2,4) = static_cast<REAL>(23) / 2880 * dy;
        MAP(values,1,0,2,5) = static_cast<REAL>(-23) / 2880 * dy;
        MAP(values,1,0,2,6) = static_cast<REAL>(-7) / 2880 * dy;
        MAP(values,1,0,2,7) = static_cast<REAL>(7) / 2880 * dy;
        MAP(values,1,1,0,0) = static_cast<REAL>(-161) / 2880 * dz;
        MAP(values,1,1,0,1) = static_cast<REAL>(161) / 2880 * dz;
        MAP(values,1,1,0,2) = static_cast<REAL>(-23) / 2880 * dz;
        MAP(values,1,1,0,3) = static_cast<REAL>(23) / 2880 * dz;
        MAP(values,1,1,0,4) = static_cast<REAL>(49) / 2880 * dz;
        MAP(values,1,1,0,5) = static_cast<REAL>(-49) / 2880 * dz;
        MAP(values,1,1,0,6) = static_cast<REAL>(7) / 2880 * dz;
        MAP(values,1,1,0,7) = static_cast<REAL>(-7) / 2880 * dz;
        MAP(values,1,1,1,0) = static_cast<REAL>(-161) / 1350 * dx / dy * dz;
        MAP(values,1,1,1,1) = static_cast<REAL>(-161) / 1350 * dx / dy * dz;
        MAP(values,1,1,1,2) = static_cast<REAL>(-23) / 1350 * dx / dy * dz;
        MAP(values,1,1,1,3) = static_cast<REAL>(-23) / 1350 * dx / dy * dz;
        MAP(values,1,1,1,4) = static_cast<REAL>(49) / 1350 * dx / dy * dz;
        MAP(values,1,1,1,5) = static_cast<REAL>(49) / 1350 * dx / dy * dz;
        MAP(values,1,1,1,6) = static_cast<REAL>(7) / 1350 * dx / dy * dz;
        MAP(values,1,1,1,7) = static_cast<REAL>(7) / 1350 * dx / dy * dz;
        MAP(values,1,1,2,0) = static_cast<REAL>(-49) / 270 * dx;
        MAP(values,1,1,2,1) = static_cast<REAL>(-49) / 270 * dx;
        MAP(values,1,1,2,2) = static_cast<REAL>(-7) / 270 * dx;
        MAP(values,1,1,2,3) = static_cast<REAL>(-7) / 270 * dx;
        MAP(values,1,1,2,4) = static_cast<REAL>(-7) / 270 * dx;
        MAP(values,1,1,2,5) = static_cast<REAL>(-7) / 270 * dx;
        MAP(values,1,1,2,6) = static_cast<REAL>(-dx) / 270;
        MAP(values,1,1,2,7) = static_cast<REAL>(-dx) / 270;
        MAP(values,1,2,0,0) = static_cast<REAL>(-161) / 2880 * dy;
        MAP(values,1,2,0,1) = static_cast<REAL>(161) / 2880 * dy;
        MAP(values,1,2,0,2) = static_cast<REAL>(49) / 2880 * dy;
        MAP(values,1,2,0,3) = static_cast<REAL>(-49) / 2880 * dy;
        MAP(values,1,2,0,4) = static_cast<REAL>(-23) / 2880 * dy;
        MAP(values,1,2,0,5) = static_cast<REAL>(23) / 2880 * dy;
        MAP(values,1,2,0,6) = static_cast<REAL>(7) / 2880 * dy;
        MAP(values,1,2,0,7) = static_cast<REAL>(-7) / 2880 * dy;
        MAP(values,1,2,1,0) = static_cast<REAL>(-49) / 270 * dx;
        MAP(values,1,2,1,1) = static_cast<REAL>(-49) / 270 * dx;
        MAP(values,1,2,1,2) = static_cast<REAL>(-7) / 270 * dx;
        MAP(values,1,2,1,3) = static_cast<REAL>(-7) / 270 * dx;
        MAP(values,1,2,1,4) = static_cast<REAL>(-7) / 270 * dx;
        MAP(values,1,2,1,5) = static_cast<REAL>(-7) / 270 * dx;
        MAP(values,1,2,1,6) = static_cast<REAL>(-dx) / 270;
        MAP(values,1,2,1,7) = static_cast<REAL>(-dx) / 270;
        MAP(values,1,2,2,0) = static_cast<REAL>(-161) / 1350 * dx * dy / dz;
        MAP(values,1,2,2,1) = static_cast<REAL>(-161) / 1350 * dx * dy / dz;
        MAP(values,1,2,2,2) = static_cast<REAL>(49) / 1350 * dx * dy / dz;
        MAP(values,1,2,2,3) = static_cast<REAL>(49) / 1350 * dx * dy / dz;
        MAP(values,1,2,2,4) = static_cast<REAL>(-23) / 1350 * dx * dy / dz;
        MAP(values,1,2,2,5) = static_cast<REAL>(-23) / 1350 * dx * dy / dz;
        MAP(values,1,2,2,6) = static_cast<REAL>(7) / 1350 * dx * dy / dz;
        MAP(values,1,2,2,7) = static_cast<REAL>(7) / 1350 * dx * dy / dz;
        MAP(values,2,0,0,0) = static_cast<REAL>(-529) / 86400 / dx * dy * dz;
        MAP(values,2,0,0,1) = static_cast<REAL>(-3703) / 86400 / dx * dy * dz;
        MAP(values,2,0,0,2) = static_cast<REAL>(161) / 86400 / dx * dy * dz;
        MAP(values,2,0,0,3) = static_cast<REAL>(1127) / 86400 / dx * dy * dz;
        MAP(values,2,0,0,4) = static_cast<REAL>(161) / 86400 / dx * dy * dz;
        MAP(values,2,0,0,5) = static_cast<REAL>(1127) / 86400 / dx * dy * dz;
        MAP(values,2,0,0,6) = static_cast<REAL>(-49) / 86400 / dx * dy * dz;
        MAP(values,2,0,0,7) = static_cast<REAL>(-343) / 86400 / dx * dy * dz;
        MAP(values,2,0,1,0) = static_cast<REAL>(161) / 17280 * dz;
        MAP(values,2,0,1,1) = static_cast<REAL>(1127) / 17280 * dz;
        MAP(values,2,0,1,2) = static_cast<REAL>(23) / 17280 * dz;
        MAP(values,2,0,1,3) = static_cast<REAL>(161) / 17280 * dz;
        MAP(values,2,0,1,4) = static_cast<REAL>(-49) / 17280 * dz;
        MAP(values,2,0,1,5) = static_cast<REAL>(-343) / 17280 * dz;
        MAP(values,2,0,1,6) = static_cast<REAL>(-7) / 17280 * dz;
        MAP(values,2,0,1,7) = static_cast<REAL>(-49) / 17280 * dz;
        MAP(values,2,0,2,0) = static_cast<REAL>(161) / 17280 * dy;
        MAP(values,2,0,2,1) = static_cast<REAL>(1127) / 17280 * dy;
        MAP(values,2,0,2,2) = static_cast<REAL>(-49) / 17280 * dy;
        MAP(values,2,0,2,3) = static_cast<REAL>(-343) / 17280 * dy;
        MAP(values,2,0,2,4) = static_cast<REAL>(23) / 17280 * dy;
        MAP(values,2,0,2,5) = static_cast<REAL>(161) / 17280 * dy;
        MAP(values,2,0,2,6) = static_cast<REAL>(-7) / 17280 * dy;
        MAP(values,2,0,2,7) = static_cast<REAL>(-49) / 17280 * dy;
        MAP(values,2,1,0,0) = static_cast<REAL>(161) / 17280 * dz;
        MAP(values,2,1,0,1) = static_cast<REAL>(1127) / 17280 * dz;
        MAP(values,2,1,0,2) = static_cast<REAL>(23) / 17280 * dz;
        MAP(values,2,1,0,3) = static_cast<REAL>(161) / 17280 * dz;
        MAP(values,2,1,0,4) = static_cast<REAL>(-49) / 17280 * dz;
        MAP(values,2,1,0,5) = static_cast<REAL>(-343) / 17280 * dz;
        MAP(values,2,1,0,6) = static_cast<REAL>(-7) / 17280 * dz;
        MAP(values,2,1,0,7) = static_cast<REAL>(-49) / 17280 * dz;
        MAP(values,2,1,1,0) = static_cast<REAL>(1127) / 86400 * dx / dy * dz;
        MAP(values,2,1,1,1) = static_cast<REAL>(-3703) / 86400 * dx / dy * dz;
        MAP(values,2,1,1,2) = static_cast<REAL>(161) / 86400 * dx / dy * dz;
        MAP(values,2,1,1,3) = static_cast<REAL>(-529) / 86400 * dx / dy * dz;
        MAP(values,2,1,1,4) = static_cast<REAL>(-343) / 86400 * dx / dy * dz;
        MAP(values,2,1,1,5) = static_cast<REAL>(1127) / 86400 * dx / dy * dz;
        MAP(values,2,1,1,6) = static_cast<REAL>(-49) / 86400 * dx / dy * dz;
        MAP(values,2,1,1,7) = static_cast<REAL>(161) / 86400 * dx / dy * dz;
        MAP(values,2,1,2,0) = static_cast<REAL>(343) / 17280 * dx;
        MAP(values,2,1,2,1) = static_cast<REAL>(-1127) / 17280 * dx;
        MAP(values,2,1,2,2) = static_cast<REAL>(49) / 17280 * dx;
        MAP(values,2,1,2,3) = static_cast<REAL>(-161) / 17280 * dx;
        MAP(values,2,1,2,4) = static_cast<REAL>(49) / 17280 * dx;
        MAP(values,2,1,2,5) = static_cast<REAL>(-161) / 17280 * dx;
        MAP(values,2,1,2,6) = static_cast<REAL>(7) / 17280 * dx;
        MAP(values,2,1,2,7) = static_cast<REAL>(-23) / 17280 * dx;
        MAP(values,2,2,0,0) = static_cast<REAL>(161) / 17280 * dy;
        MAP(values,2,2,0,1) = static_cast<REAL>(1127) / 17280 * dy;
        MAP(values,2,2,0,2) = static_cast<REAL>(-49) / 17280 * dy;
        MAP(values,2,2,0,3) = static_cast<REAL>(-343) / 17280 * dy;
        MAP(values,2,2,0,4) = static_cast<REAL>(23) / 17280 * dy;
        MAP(values,2,2,0,5) = static_cast<REAL>(161) / 17280 * dy;
        MAP(values,2,2,0,6) = static_cast<REAL>(-7) / 17280 * dy;
        MAP(values,2,2,0,7) = static_cast<REAL>(-49) / 17280 * dy;
        MAP(values,2,2,1,0) = static_cast<REAL>(343) / 17280 * dx;
        MAP(values,2,2,1,1) = static_cast<REAL>(-1127) / 17280 * dx;
        MAP(values,2,2,1,2) = static_cast<REAL>(49) / 17280 * dx;
        MAP(values,2,2,1,3) = static_cast<REAL>(-161) / 17280 * dx;
        MAP(values,2,2,1,4) = static_cast<REAL>(49) / 17280 * dx;
        MAP(values,2,2,1,5) = static_cast<REAL>(-161) / 17280 * dx;
        MAP(values,2,2,1,6) = static_cast<REAL>(7) / 17280 * dx;
        MAP(values,2,2,1,7) = static_cast<REAL>(-23) / 17280 * dx;
        MAP(values,2,2,2,0) = static_cast<REAL>(1127) / 86400 * dx * dy / dz;
        MAP(values,2,2,2,1) = static_cast<REAL>(-3703) / 86400 * dx * dy / dz;
        MAP(values,2,2,2,2) = static_cast<REAL>(-343) / 86400 * dx * dy / dz;
        MAP(values,2,2,2,3) = static_cast<REAL>(1127) / 86400 * dx * dy / dz;
        MAP(values,2,2,2,4) = static_cast<REAL>(161) / 86400 * dx * dy / dz;
        MAP(values,2,2,2,5) = static_cast<REAL>(-529) / 86400 * dx * dy / dz;
        MAP(values,2,2,2,6) = static_cast<REAL>(-49) / 86400 * dx * dy / dz;
        MAP(values,2,2,2,7) = static_cast<REAL>(161) / 86400 * dx * dy / dz;
        MAP(values,3,0,0,0) = static_cast<REAL>(-161) / 1350 / dx * dy * dz;
        MAP(values,3,0,0,1) = static_cast<REAL>(-23) / 1350 / dx * dy * dz;
        MAP(values,3,0,0,2) = static_cast<REAL>(-161) / 1350 / dx * dy * dz;
        MAP(values,3,0,0,3) = static_cast<REAL>(-23) / 1350 / dx * dy * dz;
        MAP(values,3,0,0,4) = static_cast<REAL>(49) / 1350 / dx * dy * dz;
        MAP(values,3,0,0,5) = static_cast<REAL>(7) / 1350 / dx * dy * dz;
        MAP(values,3,0,0,6) = static_cast<REAL>(49) / 1350 / dx * dy * dz;
        MAP(values,3,0,0,7) = static_cast<REAL>(7) / 1350 / dx * dy * dz;
        MAP(values,3,0,1,0) = static_cast<REAL>(-161) / 2880 * dz;
        MAP(values,3,0,1,1) = static_cast<REAL>(-23) / 2880 * dz;
        MAP(values,3,0,1,2) = static_cast<REAL>(161) / 2880 * dz;
        MAP(values,3,0,1,3) = static_cast<REAL>(23) / 2880 * dz;
        MAP(values,3,0,1,4) = static_cast<REAL>(49) / 2880 * dz;
        MAP(values,3,0,1,5) = static_cast<REAL>(7) / 2880 * dz;
        MAP(values,3,0,1,6) = static_cast<REAL>(-49) / 2880 * dz;
        MAP(values,3,0,1,7) = static_cast<REAL>(-7) / 2880 * dz;
        MAP(values,3,0,2,0) = static_cast<REAL>(-49) / 270 * dy;
        MAP(values,3,0,2,1) = static_cast<REAL>(-7) / 270 * dy;
        MAP(values,3,0,2,2) = static_cast<REAL>(-49) / 270 * dy;
        MAP(values,3,0,2,3) = static_cast<REAL>(-7) / 270 * dy;
        MAP(values,3,0,2,4) = static_cast<REAL>(-7) / 270 * dy;
        MAP(values,3,0,2,5) = static_cast<REAL>(-dy) / 270;
        MAP(values,3,0,2,6) = static_cast<REAL>(-7) / 270 * dy;
        MAP(values,3,0,2,7) = static_cast<REAL>(-dy) / 270;
        MAP(values,3,1,0,0) = static_cast<REAL>(161) / 2880 * dz;
        MAP(values,3,1,0,1) = static_cast<REAL>(23) / 2880 * dz;
        MAP(values,3,1,0,2) = static_cast<REAL>(-161) / 2880 * dz;
        MAP(values,3,1,0,3) = static_cast<REAL>(-23) / 2880 * dz;
        MAP(values,3,1,0,4) = static_cast<REAL>(-49) / 2880 * dz;
        MAP(values,3,1,0,5) = static_cast<REAL>(-7) / 2880 * dz;
        MAP(values,3,1,0,6) = static_cast<REAL>(49) / 2880 * dz;
        MAP(values,3,1,0,7) = static_cast<REAL>(7) / 2880 * dz;
        MAP(values,3,1,1,0) = static_cast<REAL>(529) / 10800 * dx / dy * dz;
        MAP(values,3,1,1,1) = static_cast<REAL>(-161) / 10800 * dx / dy * dz;
        MAP(values,3,1,1,2) = static_cast<REAL>(529) / 10800 * dx / dy * dz;
        MAP(values,3,1,1,3) = static_cast<REAL>(-161) / 10800 * dx / dy * dz;
        MAP(values,3,1,1,4) = static_cast<REAL>(-161) / 10800 * dx / dy * dz;
        MAP(values,3,1,1,5) = static_cast<REAL>(49) / 10800 * dx / dy * dz;
        MAP(values,3,1,1,6) = static_cast<REAL>(-161) / 10800 * dx / dy * dz;
        MAP(values,3,1,1,7) = static_cast<REAL>(49) / 10800 * dx / dy * dz;
        MAP(values,3,1,2,0) = static_cast<REAL>(161) / 2880 * dx;
        MAP(values,3,1,2,1) = static_cast<REAL>(-49) / 2880 * dx;
        MAP(values,3,1,2,2) = static_cast<REAL>(-161) / 2880 * dx;
        MAP(values,3,1,2,3) = static_cast<REAL>(49) / 2880 * dx;
        MAP(values,3,1,2,4) = static_cast<REAL>(23) / 2880 * dx;
        MAP(values,3,1,2,5) = static_cast<REAL>(-7) / 2880 * dx;
        MAP(values,3,1,2,6) = static_cast<REAL>(-23) / 2880 * dx;
        MAP(values,3,1,2,7) = static_cast<REAL>(7) / 2880 * dx;
        MAP(values,3,2,0,0) = static_cast<REAL>(-49) / 270 * dy;
        MAP(values,3,2,0,1) = static_cast<REAL>(-7) / 270 * dy;
        MAP(values,3,2,0,2) = static_cast<REAL>(-49) / 270 * dy;
        MAP(values,3,2,0,3) = static_cast<REAL>(-7) / 270 * dy;
        MAP(values,3,2,0,4) = static_cast<REAL>(-7) / 270 * dy;
        MAP(values,3,2,0,5) = static_cast<REAL>(-dy) / 270;
        MAP(values,3,2,0,6) = static_cast<REAL>(-7) / 270 * dy;
        MAP(values,3,2,0,7) = static_cast<REAL>(-dy) / 270;
        MAP(values,3,2,1,0) = static_cast<REAL>(-161) / 2880 * dx;
        MAP(values,3,2,1,1) = static_cast<REAL>(49) / 2880 * dx;
        MAP(values,3,2,1,2) = static_cast<REAL>(161) / 2880 * dx;
        MAP(values,3,2,1,3) = static_cast<REAL>(-49) / 2880 * dx;
        MAP(values,3,2,1,4) = static_cast<REAL>(-23) / 2880 * dx;
        MAP(values,3,2,1,5) = static_cast<REAL>(7) / 2880 * dx;
        MAP(values,3,2,1,6) = static_cast<REAL>(23) / 2880 * dx;
        MAP(values,3,2,1,7) = static_cast<REAL>(-7) / 2880 * dx;
        MAP(values,3,2,2,0) = static_cast<REAL>(-161) / 1350 * dx * dy / dz;
        MAP(values,3,2,2,1) = static_cast<REAL>(49) / 1350 * dx * dy / dz;
        MAP(values,3,2,2,2) = static_cast<REAL>(-161) / 1350 * dx * dy / dz;
        MAP(values,3,2,2,3) = static_cast<REAL>(49) / 1350 * dx * dy / dz;
        MAP(values,3,2,2,4) = static_cast<REAL>(-23) / 1350 * dx * dy / dz;
        MAP(values,3,2,2,5) = static_cast<REAL>(7) / 1350 * dx * dy / dz;
        MAP(values,3,2,2,6) = static_cast<REAL>(-23) / 1350 * dx * dy / dz;
        MAP(values,3,2,2,7) = static_cast<REAL>(7) / 1350 * dx * dy / dz;
        MAP(values,4,0,0,0) = static_cast<REAL>(92) / 675 / dx * dy * dz;
        MAP(values,4,0,0,1) = static_cast<REAL>(92) / 675 / dx * dy * dz;
        MAP(values,4,0,0,2) = static_cast<REAL>(92) / 675 / dx * dy * dz;
        MAP(values,4,0,0,3) = static_cast<REAL>(92) / 675 / dx * dy * dz;
        MAP(values,4,0,0,4) = static_cast<REAL>(-28) / 675 / dx * dy * dz;
        MAP(values,4,0,0,5) = static_cast<REAL>(-28) / 675 / dx * dy * dz;
        MAP(values,4,0,0,6) = static_cast<REAL>(-28) / 675 / dx * dy * dz;
        MAP(values,4,0,0,7) = static_cast<REAL>(-28) / 675 / dx * dy * dz;
        MAP(values,4,0,1,0) = static_cast<REAL>(23) / 480 * dz;
        MAP(values,4,0,1,1) = static_cast<REAL>(-23) / 480 * dz;
        MAP(values,4,0,1,2) = static_cast<REAL>(-23) / 480 * dz;
        MAP(values,4,0,1,3) = static_cast<REAL>(23) / 480 * dz;
        MAP(values,4,0,1,4) = static_cast<REAL>(-7) / 480 * dz;
        MAP(values,4,0,1,5) = static_cast<REAL>(7) / 480 * dz;
        MAP(values,4,0,1,6) = static_cast<REAL>(7) / 480 * dz;
        MAP(values,4,0,1,7) = static_cast<REAL>(-7) / 480 * dz;
        MAP(values,4,0,2,0) = static_cast<REAL>(7) / 45 * dy;
        MAP(values,4,0,2,1) = static_cast<REAL>(-7) / 45 * dy;
        MAP(values,4,0,2,2) = static_cast<REAL>(7) / 45 * dy;
        MAP(values,4,0,2,3) = static_cast<REAL>(-7) / 45 * dy;
        MAP(values,4,0,2,4) = static_cast<REAL>(dy) / 45;
        MAP(values,4,0,2,5) = static_cast<REAL>(-dy) / 45;
        MAP(values,4,0,2,6) = static_cast<REAL>(dy) / 45;
        MAP(values,4,0,2,7) = static_cast<REAL>(-dy) / 45;
        MAP(values,4,1,0,0) = static_cast<REAL>(23) / 480 * dz;
        MAP(values,4,1,0,1) = static_cast<REAL>(-23) / 480 * dz;
        MAP(values,4,1,0,2) = static_cast<REAL>(-23) / 480 * dz;
        MAP(values,4,1,0,3) = static_cast<REAL>(23) / 480 * dz;
        MAP(values,4,1,0,4) = static_cast<REAL>(-7) / 480 * dz;
        MAP(values,4,1,0,5) = static_cast<REAL>(7) / 480 * dz;
        MAP(values,4,1,0,6) = static_cast<REAL>(7) / 480 * dz;
        MAP(values,4,1,0,7) = static_cast<REAL>(-7) / 480 * dz;
        MAP(values,4,1,1,0) = static_cast<REAL>(92) / 675 * dx / dy * dz;
        MAP(values,4,1,1,1) = static_cast<REAL>(92) / 675 * dx / dy * dz;
        MAP(values,4,1,1,2) = static_cast<REAL>(92) / 675 * dx / dy * dz;
        MAP(values,4,1,1,3) = static_cast<REAL>(92) / 675 * dx / dy * dz;
        MAP(values,4,1,1,4) = static_cast<REAL>(-28) / 675 * dx / dy * dz;
        MAP(values,4,1,1,5) = static_cast<REAL>(-28) / 675 * dx / dy * dz;
        MAP(values,4,1,1,6) = static_cast<REAL>(-28) / 675 * dx / dy * dz;
        MAP(values,4,1,1,7) = static_cast<REAL>(-28) / 675 * dx / dy * dz;
        MAP(values,4,1,2,0) = static_cast<REAL>(7) / 45 * dx;
        MAP(values,4,1,2,1) = static_cast<REAL>(7) / 45 * dx;
        MAP(values,4,1,2,2) = static_cast<REAL>(-7) / 45 * dx;
        MAP(values,4,1,2,3) = static_cast<REAL>(-7) / 45 * dx;
        MAP(values,4,1,2,4) = static_cast<REAL>(dx) / 45;
        MAP(values,4,1,2,5) = static_cast<REAL>(dx) / 45;
        MAP(values,4,1,2,6) = static_cast<REAL>(-dx) / 45;
        MAP(values,4,1,2,7) = static_cast<REAL>(-dx) / 45;
        MAP(values,4,2,0,0) = static_cast<REAL>(-7) / 45 * dy;
        MAP(values,4,2,0,1) = static_cast<REAL>(7) / 45 * dy;
        MAP(values,4,2,0,2) = static_cast<REAL>(-7) / 45 * dy;
        MAP(values,4,2,0,3) = static_cast<REAL>(7) / 45 * dy;
        MAP(values,4,2,0,4) = static_cast<REAL>(-dy) / 45;
        MAP(values,4,2,0,5) = static_cast<REAL>(dy) / 45;
        MAP(values,4,2,0,6) = static_cast<REAL>(-dy) / 45;
        MAP(values,4,2,0,7) = static_cast<REAL>(dy) / 45;
        MAP(values,4,2,1,0) = static_cast<REAL>(-7) / 45 * dx;
        MAP(values,4,2,1,1) = static_cast<REAL>(-7) / 45 * dx;
        MAP(values,4,2,1,2) = static_cast<REAL>(7) / 45 * dx;
        MAP(values,4,2,1,3) = static_cast<REAL>(7) / 45 * dx;
        MAP(values,4,2,1,4) = static_cast<REAL>(-dx) / 45;
        MAP(values,4,2,1,5) = static_cast<REAL>(-dx) / 45;
        MAP(values,4,2,1,6) = static_cast<REAL>(dx) / 45;
        MAP(values,4,2,1,7) = static_cast<REAL>(dx) / 45;
        MAP(values,4,2,2,0) = static_cast<REAL>(-224) / 675 * dx * dy / dz;
        MAP(values,4,2,2,1) = static_cast<REAL>(-224) / 675 * dx * dy / dz;
        MAP(values,4,2,2,2) = static_cast<REAL>(-224) / 675 * dx * dy / dz;
        MAP(values,4,2,2,3) = static_cast<REAL>(-224) / 675 * dx * dy / dz;
        MAP(values,4,2,2,4) = static_cast<REAL>(-32) / 675 * dx * dy / dz;
        MAP(values,4,2,2,5) = static_cast<REAL>(-32) / 675 * dx * dy / dz;
        MAP(values,4,2,2,6) = static_cast<REAL>(-32) / 675 * dx * dy / dz;
        MAP(values,4,2,2,7) = static_cast<REAL>(-32) / 675 * dx * dy / dz;
        MAP(values,5,0,0,0) = static_cast<REAL>(-23) / 1350 / dx * dy * dz;
        MAP(values,5,0,0,1) = static_cast<REAL>(-161) / 1350 / dx * dy * dz;
        MAP(values,5,0,0,2) = static_cast<REAL>(-23) / 1350 / dx * dy * dz;
        MAP(values,5,0,0,3) = static_cast<REAL>(-161) / 1350 / dx * dy * dz;
        MAP(values,5,0,0,4) = static_cast<REAL>(7) / 1350 / dx * dy * dz;
        MAP(values,5,0,0,5) = static_cast<REAL>(49) / 1350 / dx * dy * dz;
        MAP(values,5,0,0,6) = static_cast<REAL>(7) / 1350 / dx * dy * dz;
        MAP(values,5,0,0,7) = static_cast<REAL>(49) / 1350 / dx * dy * dz;
        MAP(values,5,0,1,0) = static_cast<REAL>(23) / 2880 * dz;
        MAP(values,5,0,1,1) = static_cast<REAL>(161) / 2880 * dz;
        MAP(values,5,0,1,2) = static_cast<REAL>(-23) / 2880 * dz;
        MAP(values,5,0,1,3) = static_cast<REAL>(-161) / 2880 * dz;
        MAP(values,5,0,1,4) = static_cast<REAL>(-7) / 2880 * dz;
        MAP(values,5,0,1,5) = static_cast<REAL>(-49) / 2880 * dz;
        MAP(values,5,0,1,6) = static_cast<REAL>(7) / 2880 * dz;
        MAP(values,5,0,1,7) = static_cast<REAL>(49) / 2880 * dz;
        MAP(values,5,0,2,0) = static_cast<REAL>(7) / 270 * dy;
        MAP(values,5,0,2,1) = static_cast<REAL>(49) / 270 * dy;
        MAP(values,5,0,2,2) = static_cast<REAL>(7) / 270 * dy;
        MAP(values,5,0,2,3) = static_cast<REAL>(49) / 270 * dy;
        MAP(values,5,0,2,4) = static_cast<REAL>(dy) / 270;
        MAP(values,5,0,2,5) = static_cast<REAL>(7) / 270 * dy;
        MAP(values,5,0,2,6) = static_cast<REAL>(dy) / 270;
        MAP(values,5,0,2,7) = static_cast<REAL>(7) / 270 * dy;
        MAP(values,5,1,0,0) = static_cast<REAL>(-23) / 2880 * dz;
        MAP(values,5,1,0,1) = static_cast<REAL>(-161) / 2880 * dz;
        MAP(values,5,1,0,2) = static_cast<REAL>(23) / 2880 * dz;
        MAP(values,5,1,0,3) = static_cast<REAL>(161) / 2880 * dz;
        MAP(values,5,1,0,4) = static_cast<REAL>(7) / 2880 * dz;
        MAP(values,5,1,0,5) = static_cast<REAL>(49) / 2880 * dz;
        MAP(values,5,1,0,6) = static_cast<REAL>(-7) / 2880 * dz;
        MAP(values,5,1,0,7) = static_cast<REAL>(-49) / 2880 * dz;
        MAP(values,5,1,1,0) = static_cast<REAL>(-161) / 10800 * dx / dy * dz;
        MAP(values,5,1,1,1) = static_cast<REAL>(529) / 10800 * dx / dy * dz;
        MAP(values,5,1,1,2) = static_cast<REAL>(-161) / 10800 * dx / dy * dz;
        MAP(values,5,1,1,3) = static_cast<REAL>(529) / 10800 * dx / dy * dz;
        MAP(values,5,1,1,4) = static_cast<REAL>(49) / 10800 * dx / dy * dz;
        MAP(values,5,1,1,5) = static_cast<REAL>(-161) / 10800 * dx / dy * dz;
        MAP(values,5,1,1,6) = static_cast<REAL>(49) / 10800 * dx / dy * dz;
        MAP(values,5,1,1,7) = static_cast<REAL>(-161) / 10800 * dx / dy * dz;
        MAP(values,5,1,2,0) = static_cast<REAL>(-49) / 2880 * dx;
        MAP(values,5,1,2,1) = static_cast<REAL>(161) / 2880 * dx;
        MAP(values,5,1,2,2) = static_cast<REAL>(49) / 2880 * dx;
        MAP(values,5,1,2,3) = static_cast<REAL>(-161) / 2880 * dx;
        MAP(values,5,1,2,4) = static_cast<REAL>(-7) / 2880 * dx;
        MAP(values,5,1,2,5) = static_cast<REAL>(23) / 2880 * dx;
        MAP(values,5,1,2,6) = static_cast<REAL>(7) / 2880 * dx;
        MAP(values,5,1,2,7) = static_cast<REAL>(-23) / 2880 * dx;
        MAP(values,5,2,0,0) = static_cast<REAL>(7) / 270 * dy;
        MAP(values,5,2,0,1) = static_cast<REAL>(49) / 270 * dy;
        MAP(values,5,2,0,2) = static_cast<REAL>(7) / 270 * dy;
        MAP(values,5,2,0,3) = static_cast<REAL>(49) / 270 * dy;
        MAP(values,5,2,0,4) = static_cast<REAL>(dy) / 270;
        MAP(values,5,2,0,5) = static_cast<REAL>(7) / 270 * dy;
        MAP(values,5,2,0,6) = static_cast<REAL>(dy) / 270;
        MAP(values,5,2,0,7) = static_cast<REAL>(7) / 270 * dy;
        MAP(values,5,2,1,0) = static_cast<REAL>(49) / 2880 * dx;
        MAP(values,5,2,1,1) = static_cast<REAL>(-161) / 2880 * dx;
        MAP(values,5,2,1,2) = static_cast<REAL>(-49) / 2880 * dx;
        MAP(values,5,2,1,3) = static_cast<REAL>(161) / 2880 * dx;
        MAP(values,5,2,1,4) = static_cast<REAL>(7) / 2880 * dx;
        MAP(values,5,2,1,5) = static_cast<REAL>(-23) / 2880 * dx;
        MAP(values,5,2,1,6) = static_cast<REAL>(-7) / 2880 * dx;
        MAP(values,5,2,1,7) = static_cast<REAL>(23) / 2880 * dx;
        MAP(values,5,2,2,0) = static_cast<REAL>(49) / 1350 * dx * dy / dz;
        MAP(values,5,2,2,1) = static_cast<REAL>(-161) / 1350 * dx * dy / dz;
        MAP(values,5,2,2,2) = static_cast<REAL>(49) / 1350 * dx * dy / dz;
        MAP(values,5,2,2,3) = static_cast<REAL>(-161) / 1350 * dx * dy / dz;
        MAP(values,5,2,2,4) = static_cast<REAL>(7) / 1350 * dx * dy / dz;
        MAP(values,5,2,2,5) = static_cast<REAL>(-23) / 1350 * dx * dy / dz;
        MAP(values,5,2,2,6) = static_cast<REAL>(7) / 1350 * dx * dy / dz;
        MAP(values,5,2,2,7) = static_cast<REAL>(-23) / 1350 * dx * dy / dz;
        MAP(values,6,0,0,0) = static_cast<REAL>(1127) / 86400 / dx * dy * dz;
        MAP(values,6,0,0,1) = static_cast<REAL>(161) / 86400 / dx * dy * dz;
        MAP(values,6,0,0,2) = static_cast<REAL>(-3703) / 86400 / dx * dy * dz;
        MAP(values,6,0,0,3) = static_cast<REAL>(-529) / 86400 / dx * dy * dz;
        MAP(values,6,0,0,4) = static_cast<REAL>(-343) / 86400 / dx * dy * dz;
        MAP(values,6,0,0,5) = static_cast<REAL>(-49) / 86400 / dx * dy * dz;
        MAP(values,6,0,0,6) = static_cast<REAL>(1127) / 86400 / dx * dy * dz;
        MAP(values,6,0,0,7) = static_cast<REAL>(161) / 86400 / dx * dy * dz;
        MAP(values,6,0,1,0) = static_cast<REAL>(161) / 17280 * dz;
        MAP(values,6,0,1,1) = static_cast<REAL>(23) / 17280 * dz;
        MAP(values,6,0,1,2) = static_cast<REAL>(1127) / 17280 * dz;
        MAP(values,6,0,1,3) = static_cast<REAL>(161) / 17280 * dz;
        MAP(values,6,0,1,4) = static_cast<REAL>(-49) / 17280 * dz;
        MAP(values,6,0,1,5) = static_cast<REAL>(-7) / 17280 * dz;
        MAP(values,6,0,1,6) = static_cast<REAL>(-343) / 17280 * dz;
        MAP(values,6,0,1,7) = static_cast<REAL>(-49) / 17280 * dz;
        MAP(values,6,0,2,0) = static_cast<REAL>(343) / 17280 * dy;
        MAP(values,6,0,2,1) = static_cast<REAL>(49) / 17280 * dy;
        MAP(values,6,0,2,2) = static_cast<REAL>(-1127) / 17280 * dy;
        MAP(values,6,0,2,3) = static_cast<REAL>(-161) / 17280 * dy;
        MAP(values,6,0,2,4) = static_cast<REAL>(49) / 17280 * dy;
        MAP(values,6,0,2,5) = static_cast<REAL>(7) / 17280 * dy;
        MAP(values,6,0,2,6) = static_cast<REAL>(-161) / 17280 * dy;
        MAP(values,6,0,2,7) = static_cast<REAL>(-23) / 17280 * dy;
        MAP(values,6,1,0,0) = static_cast<REAL>(161) / 17280 * dz;
        MAP(values,6,1,0,1) = static_cast<REAL>(23) / 17280 * dz;
        MAP(values,6,1,0,2) = static_cast<REAL>(1127) / 17280 * dz;
        MAP(values,6,1,0,3) = static_cast<REAL>(161) / 17280 * dz;
        MAP(values,6,1,0,4) = static_cast<REAL>(-49) / 17280 * dz;
        MAP(values,6,1,0,5) = static_cast<REAL>(-7) / 17280 * dz;
        MAP(values,6,1,0,6) = static_cast<REAL>(-343) / 17280 * dz;
        MAP(values,6,1,0,7) = static_cast<REAL>(-49) / 17280 * dz;
        MAP(values,6,1,1,0) = static_cast<REAL>(-529) / 86400 * dx / dy * dz;
        MAP(values,6,1,1,1) = static_cast<REAL>(161) / 86400 * dx / dy * dz;
        MAP(values,6,1,1,2) = static_cast<REAL>(-3703) / 86400 * dx / dy * dz;
        MAP(values,6,1,1,3) = static_cast<REAL>(1127) / 86400 * dx / dy * dz;
        MAP(values,6,1,1,4) = static_cast<REAL>(161) / 86400 * dx / dy * dz;
        MAP(values,6,1,1,5) = static_cast<REAL>(-49) / 86400 * dx / dy * dz;
        MAP(values,6,1,1,6) = static_cast<REAL>(1127) / 86400 * dx / dy * dz;
        MAP(values,6,1,1,7) = static_cast<REAL>(-343) / 86400 * dx / dy * dz;
        MAP(values,6,1,2,0) = static_cast<REAL>(161) / 17280 * dx;
        MAP(values,6,1,2,1) = static_cast<REAL>(-49) / 17280 * dx;
        MAP(values,6,1,2,2) = static_cast<REAL>(1127) / 17280 * dx;
        MAP(values,6,1,2,3) = static_cast<REAL>(-343) / 17280 * dx;
        MAP(values,6,1,2,4) = static_cast<REAL>(23) / 17280 * dx;
        MAP(values,6,1,2,5) = static_cast<REAL>(-7) / 17280 * dx;
        MAP(values,6,1,2,6) = static_cast<REAL>(161) / 17280 * dx;
        MAP(values,6,1,2,7) = static_cast<REAL>(-49) / 17280 * dx;
        MAP(values,6,2,0,0) = static_cast<REAL>(343) / 17280 * dy;
        MAP(values,6,2,0,1) = static_cast<REAL>(49) / 17280 * dy;
        MAP(values,6,2,0,2) = static_cast<REAL>(-1127) / 17280 * dy;
        MAP(values,6,2,0,3) = static_cast<REAL>(-161) / 17280 * dy;
        MAP(values,6,2,0,4) = static_cast<REAL>(49) / 17280 * dy;
        MAP(values,6,2,0,5) = static_cast<REAL>(7) / 17280 * dy;
        MAP(values,6,2,0,6) = static_cast<REAL>(-161) / 17280 * dy;
        MAP(values,6,2,0,7) = static_cast<REAL>(-23) / 17280 * dy;
        MAP(values,6,2,1,0) = static_cast<REAL>(161) / 17280 * dx;
        MAP(values,6,2,1,1) = static_cast<REAL>(-49) / 17280 * dx;
        MAP(values,6,2,1,2) = static_cast<REAL>(1127) / 17280 * dx;
        MAP(values,6,2,1,3) = static_cast<REAL>(-343) / 17280 * dx;
        MAP(values,6,2,1,4) = static_cast<REAL>(23) / 17280 * dx;
        MAP(values,6,2,1,5) = static_cast<REAL>(-7) / 17280 * dx;
        MAP(values,6,2,1,6) = static_cast<REAL>(161) / 17280 * dx;
        MAP(values,6,2,1,7) = static_cast<REAL>(-49) / 17280 * dx;
        MAP(values,6,2,2,0) = static_cast<REAL>(1127) / 86400 * dx * dy / dz;
        MAP(values,6,2,2,1) = static_cast<REAL>(-343) / 86400 * dx * dy / dz;
        MAP(values,6,2,2,2) = static_cast<REAL>(-3703) / 86400 * dx * dy / dz;
        MAP(values,6,2,2,3) = static_cast<REAL>(1127) / 86400 * dx * dy / dz;
        MAP(values,6,2,2,4) = static_cast<REAL>(161) / 86400 * dx * dy / dz;
        MAP(values,6,2,2,5) = static_cast<REAL>(-49) / 86400 * dx * dy / dz;
        MAP(values,6,2,2,6) = static_cast<REAL>(-529) / 86400 * dx * dy / dz;
        MAP(values,6,2,2,7) = static_cast<REAL>(161) / 86400 * dx * dy / dz;
        MAP(values,7,0,0,0) = static_cast<REAL>(-161) / 10800 / dx * dy * dz;
        MAP(values,7,0,0,1) = static_cast<REAL>(-161) / 10800 / dx * dy * dz;
        MAP(values,7,0,0,2) = static_cast<REAL>(529) / 10800 / dx * dy * dz;
        MAP(values,7,0,0,3) = static_cast<REAL>(529) / 10800 / dx * dy * dz;
        MAP(values,7,0,0,4) = static_cast<REAL>(49) / 10800 / dx * dy * dz;
        MAP(values,7,0,0,5) = static_cast<REAL>(49) / 10800 / dx * dy * dz;
        MAP(values,7,0,0,6) = static_cast<REAL>(-161) / 10800 / dx * dy * dz;
        MAP(values,7,0,0,7) = static_cast<REAL>(-161) / 10800 / dx * dy * dz;
        MAP(values,7,0,1,0) = static_cast<REAL>(-23) / 2880 * dz;
        MAP(values,7,0,1,1) = static_cast<REAL>(23) / 2880 * dz;
        MAP(values,7,0,1,2) = static_cast<REAL>(-161) / 2880 * dz;
        MAP(values,7,0,1,3) = static_cast<REAL>(161) / 2880 * dz;
        MAP(values,7,0,1,4) = static_cast<REAL>(7) / 2880 * dz;
        MAP(values,7,0,1,5) = static_cast<REAL>(-7) / 2880 * dz;
        MAP(values,7,0,1,6) = static_cast<REAL>(49) / 2880 * dz;
        MAP(values,7,0,1,7) = static_cast<REAL>(-49) / 2880 * dz;
        MAP(values,7,0,2,0) = static_cast<REAL>(-49) / 2880 * dy;
        MAP(values,7,0,2,1) = static_cast<REAL>(49) / 2880 * dy;
        MAP(values,7,0,2,2) = static_cast<REAL>(161) / 2880 * dy;
        MAP(values,7,0,2,3) = static_cast<REAL>(-161) / 2880 * dy;
        MAP(values,7,0,2,4) = static_cast<REAL>(-7) / 2880 * dy;
        MAP(values,7,0,2,5) = static_cast<REAL>(7) / 2880 * dy;
        MAP(values,7,0,2,6) = static_cast<REAL>(23) / 2880 * dy;
        MAP(values,7,0,2,7) = static_cast<REAL>(-23) / 2880 * dy;
        MAP(values,7,1,0,0) = static_cast<REAL>(23) / 2880 * dz;
        MAP(values,7,1,0,1) = static_cast<REAL>(-23) / 2880 * dz;
        MAP(values,7,1,0,2) = static_cast<REAL>(161) / 2880 * dz;
        MAP(values,7,1,0,3) = static_cast<REAL>(-161) / 2880 * dz;
        MAP(values,7,1,0,4) = static_cast<REAL>(-7) / 2880 * dz;
        MAP(values,7,1,0,5) = static_cast<REAL>(7) / 2880 * dz;
        MAP(values,7,1,0,6) = static_cast<REAL>(-49) / 2880 * dz;
        MAP(values,7,1,0,7) = static_cast<REAL>(49) / 2880 * dz;
        MAP(values,7,1,1,0) = static_cast<REAL>(-23) / 1350 * dx / dy * dz;
        MAP(values,7,1,1,1) = static_cast<REAL>(-23) / 1350 * dx / dy * dz;
        MAP(values,7,1,1,2) = static_cast<REAL>(-161) / 1350 * dx / dy * dz;
        MAP(values,7,1,1,3) = static_cast<REAL>(-161) / 1350 * dx / dy * dz;
        MAP(values,7,1,1,4) = static_cast<REAL>(7) / 1350 * dx / dy * dz;
        MAP(values,7,1,1,5) = static_cast<REAL>(7) / 1350 * dx / dy * dz;
        MAP(values,7,1,1,6) = static_cast<REAL>(49) / 1350 * dx / dy * dz;
        MAP(values,7,1,1,7) = static_cast<REAL>(49) / 1350 * dx / dy * dz;
        MAP(values,7,1,2,0) = static_cast<REAL>(7) / 270 * dx;
        MAP(values,7,1,2,1) = static_cast<REAL>(7) / 270 * dx;
        MAP(values,7,1,2,2) = static_cast<REAL>(49) / 270 * dx;
        MAP(values,7,1,2,3) = static_cast<REAL>(49) / 270 * dx;
        MAP(values,7,1,2,4) = static_cast<REAL>(dx) / 270;
        MAP(values,7,1,2,5) = static_cast<REAL>(dx) / 270;
        MAP(values,7,1,2,6) = static_cast<REAL>(7) / 270 * dx;
        MAP(values,7,1,2,7) = static_cast<REAL>(7) / 270 * dx;
        MAP(values,7,2,0,0) = static_cast<REAL>(49) / 2880 * dy;
        MAP(values,7,2,0,1) = static_cast<REAL>(-49) / 2880 * dy;
        MAP(values,7,2,0,2) = static_cast<REAL>(-161) / 2880 * dy;
        MAP(values,7,2,0,3) = static_cast<REAL>(161) / 2880 * dy;
        MAP(values,7,2,0,4) = static_cast<REAL>(7) / 2880 * dy;
        MAP(values,7,2,0,5) = static_cast<REAL>(-7) / 2880 * dy;
        MAP(values,7,2,0,6) = static_cast<REAL>(-23) / 2880 * dy;
        MAP(values,7,2,0,7) = static_cast<REAL>(23) / 2880 * dy;
        MAP(values,7,2,1,0) = static_cast<REAL>(7) / 270 * dx;
        MAP(values,7,2,1,1) = static_cast<REAL>(7) / 270 * dx;
        MAP(values,7,2,1,2) = static_cast<REAL>(49) / 270 * dx;
        MAP(values,7,2,1,3) = static_cast<REAL>(49) / 270 * dx;
        MAP(values,7,2,1,4) = static_cast<REAL>(dx) / 270;
        MAP(values,7,2,1,5) = static_cast<REAL>(dx) / 270;
        MAP(values,7,2,1,6) = static_cast<REAL>(7) / 270 * dx;
        MAP(values,7,2,1,7) = static_cast<REAL>(7) / 270 * dx;
        MAP(values,7,2,2,0) = static_cast<REAL>(49) / 1350 * dx * dy / dz;
        MAP(values,7,2,2,1) = static_cast<REAL>(49) / 1350 * dx * dy / dz;
        MAP(values,7,2,2,2) = static_cast<REAL>(-161) / 1350 * dx * dy / dz;
        MAP(values,7,2,2,3) = static_cast<REAL>(-161) / 1350 * dx * dy / dz;
        MAP(values,7,2,2,4) = static_cast<REAL>(7) / 1350 * dx * dy / dz;
        MAP(values,7,2,2,5) = static_cast<REAL>(7) / 1350 * dx * dy / dz;
        MAP(values,7,2,2,6) = static_cast<REAL>(-23) / 1350 * dx * dy / dz;
        MAP(values,7,2,2,7) = static_cast<REAL>(-23) / 1350 * dx * dy / dz;
        MAP(values,8,0,0,0) = static_cast<REAL>(161) / 86400 / dx * dy * dz;
        MAP(values,8,0,0,1) = static_cast<REAL>(1127) / 86400 / dx * dy * dz;
        MAP(values,8,0,0,2) = static_cast<REAL>(-529) / 86400 / dx * dy * dz;
        MAP(values,8,0,0,3) = static_cast<REAL>(-3703) / 86400 / dx * dy * dz;
        MAP(values,8,0,0,4) = static_cast<REAL>(-49) / 86400 / dx * dy * dz;
        MAP(values,8,0,0,5) = static_cast<REAL>(-343) / 86400 / dx * dy * dz;
        MAP(values,8,0,0,6) = static_cast<REAL>(161) / 86400 / dx * dy * dz;
        MAP(values,8,0,0,7) = static_cast<REAL>(1127) / 86400 / dx * dy * dz;
        MAP(values,8,0,1,0) = static_cast<REAL>(-23) / 17280 * dz;
        MAP(values,8,0,1,1) = static_cast<REAL>(-161) / 17280 * dz;
        MAP(values,8,0,1,2) = static_cast<REAL>(-161) / 17280 * dz;
        MAP(values,8,0,1,3) = static_cast<REAL>(-1127) / 17280 * dz;
        MAP(values,8,0,1,4) = static_cast<REAL>(7) / 17280 * dz;
        MAP(values,8,0,1,5) = static_cast<REAL>(49) / 17280 * dz;
        MAP(values,8,0,1,6) = static_cast<REAL>(49) / 17280 * dz;
        MAP(values,8,0,1,7) = static_cast<REAL>(343) / 17280 * dz;
        MAP(values,8,0,2,0) = static_cast<REAL>(-49) / 17280 * dy;
        MAP(values,8,0,2,1) = static_cast<REAL>(-343) / 17280 * dy;
        MAP(values,8,0,2,2) = static_cast<REAL>(161) / 17280 * dy;
        MAP(values,8,0,2,3) = static_cast<REAL>(1127) / 17280 * dy;
        MAP(values,8,0,2,4) = static_cast<REAL>(-7) / 17280 * dy;
        MAP(values,8,0,2,5) = static_cast<REAL>(-49) / 17280 * dy;
        MAP(values,8,0,2,6) = static_cast<REAL>(23) / 17280 * dy;
        MAP(values,8,0,2,7) = static_cast<REAL>(161) / 17280 * dy;
        MAP(values,8,1,0,0) = static_cast<REAL>(-23) / 17280 * dz;
        MAP(values,8,1,0,1) = static_cast<REAL>(-161) / 17280 * dz;
        MAP(values,8,1,0,2) = static_cast<REAL>(-161) / 17280 * dz;
        MAP(values,8,1,0,3) = static_cast<REAL>(-1127) / 17280 * dz;
        MAP(values,8,1,0,4) = static_cast<REAL>(7) / 17280 * dz;
        MAP(values,8,1,0,5) = static_cast<REAL>(49) / 17280 * dz;
        MAP(values,8,1,0,6) = static_cast<REAL>(49) / 17280 * dz;
        MAP(values,8,1,0,7) = static_cast<REAL>(343) / 17280 * dz;
        MAP(values,8,1,1,0) = static_cast<REAL>(161) / 86400 * dx / dy * dz;
        MAP(values,8,1,1,1) = static_cast<REAL>(-529) / 86400 * dx / dy * dz;
        MAP(values,8,1,1,2) = static_cast<REAL>(1127) / 86400 * dx / dy * dz;
        MAP(values,8,1,1,3) = static_cast<REAL>(-3703) / 86400 * dx / dy * dz;
        MAP(values,8,1,1,4) = static_cast<REAL>(-49) / 86400 * dx / dy * dz;
        MAP(values,8,1,1,5) = static_cast<REAL>(161) / 86400 * dx / dy * dz;
        MAP(values,8,1,1,6) = static_cast<REAL>(-343) / 86400 * dx / dy * dz;
        MAP(values,8,1,1,7) = static_cast<REAL>(1127) / 86400 * dx / dy * dz;
        MAP(values,8,1,2,0) = static_cast<REAL>(-49) / 17280 * dx;
        MAP(values,8,1,2,1) = static_cast<REAL>(161) / 17280 * dx;
        MAP(values,8,1,2,2) = static_cast<REAL>(-343) / 17280 * dx;
        MAP(values,8,1,2,3) = static_cast<REAL>(1127) / 17280 * dx;
        MAP(values,8,1,2,4) = static_cast<REAL>(-7) / 17280 * dx;
        MAP(values,8,1,2,5) = static_cast<REAL>(23) / 17280 * dx;
        MAP(values,8,1,2,6) = static_cast<REAL>(-49) / 17280 * dx;
        MAP(values,8,1,2,7) = static_cast<REAL>(161) / 17280 * dx;
        MAP(values,8,2,0,0) = static_cast<REAL>(-49) / 17280 * dy;
        MAP(values,8,2,0,1) = static_cast<REAL>(-343) / 17280 * dy;
        MAP(values,8,2,0,2) = static_cast<REAL>(161) / 17280 * dy;
        MAP(values,8,2,0,3) = static_cast<REAL>(1127) / 17280 * dy;
        MAP(values,8,2,0,4) = static_cast<REAL>(-7) / 17280 * dy;
        MAP(values,8,2,0,5) = static_cast<REAL>(-49) / 17280 * dy;
        MAP(values,8,2,0,6) = static_cast<REAL>(23) / 17280 * dy;
        MAP(values,8,2,0,7) = static_cast<REAL>(161) / 17280 * dy;
        MAP(values,8,2,1,0) = static_cast<REAL>(-49) / 17280 * dx;
        MAP(values,8,2,1,1) = static_cast<REAL>(161) / 17280 * dx;
        MAP(values,8,2,1,2) = static_cast<REAL>(-343) / 17280 * dx;
        MAP(values,8,2,1,3) = static_cast<REAL>(1127) / 17280 * dx;
        MAP(values,8,2,1,4) = static_cast<REAL>(-7) / 17280 * dx;
        MAP(values,8,2,1,5) = static_cast<REAL>(23) / 17280 * dx;
        MAP(values,8,2,1,6) = static_cast<REAL>(-49) / 17280 * dx;
        MAP(values,8,2,1,7) = static_cast<REAL>(161) / 17280 * dx;
        MAP(values,8,2,2,0) = static_cast<REAL>(-343) / 86400 * dx * dy / dz;
        MAP(values,8,2,2,1) = static_cast<REAL>(1127) / 86400 * dx * dy / dz;
        MAP(values,8,2,2,2) = static_cast<REAL>(1127) / 86400 * dx * dy / dz;
        MAP(values,8,2,2,3) = static_cast<REAL>(-3703) / 86400 * dx * dy / dz;
        MAP(values,8,2,2,4) = static_cast<REAL>(-49) / 86400 * dx * dy / dz;
        MAP(values,8,2,2,5) = static_cast<REAL>(161) / 86400 * dx * dy / dz;
        MAP(values,8,2,2,6) = static_cast<REAL>(161) / 86400 * dx * dy / dz;
        MAP(values,8,2,2,7) = static_cast<REAL>(-529) / 86400 * dx * dy / dz;
        MAP(values,9,0,0,0) = static_cast<REAL>(-161) / 1350 / dx * dy * dz;
        MAP(values,9,0,0,1) = static_cast<REAL>(-23) / 1350 / dx * dy * dz;
        MAP(values,9,0,0,2) = static_cast<REAL>(49) / 1350 / dx * dy * dz;
        MAP(values,9,0,0,3) = static_cast<REAL>(7) / 1350 / dx * dy * dz;
        MAP(values,9,0,0,4) = static_cast<REAL>(-161) / 1350 / dx * dy * dz;
        MAP(values,9,0,0,5) = static_cast<REAL>(-23) / 1350 / dx * dy * dz;
        MAP(values,9,0,0,6) = static_cast<REAL>(49) / 1350 / dx * dy * dz;
        MAP(values,9,0,0,7) = static_cast<REAL>(7) / 1350 / dx * dy * dz;
        MAP(values,9,0,1,0) = static_cast<REAL>(-49) / 270 * dz;
        MAP(values,9,0,1,1) = static_cast<REAL>(-7) / 270 * dz;
        MAP(values,9,0,1,2) = static_cast<REAL>(-7) / 270 * dz;
        MAP(values,9,0,1,3) = static_cast<REAL>(-dz) / 270;
        MAP(values,9,0,1,4) = static_cast<REAL>(-49) / 270 * dz;
        MAP(values,9,0,1,5) = static_cast<REAL>(-7) / 270 * dz;
        MAP(values,9,0,1,6) = static_cast<REAL>(-7) / 270 * dz;
        MAP(values,9,0,1,7) = static_cast<REAL>(-dz) / 270;
        MAP(values,9,0,2,0) = static_cast<REAL>(-161) / 2880 * dy;
        MAP(values,9,0,2,1) = static_cast<REAL>(-23) / 2880 * dy;
        MAP(values,9,0,2,2) = static_cast<REAL>(49) / 2880 * dy;
        MAP(values,9,0,2,3) = static_cast<REAL>(7) / 2880 * dy;
        MAP(values,9,0,2,4) = static_cast<REAL>(161) / 2880 * dy;
        MAP(values,9,0,2,5) = static_cast<REAL>(23) / 2880 * dy;
        MAP(values,9,0,2,6) = static_cast<REAL>(-49) / 2880 * dy;
        MAP(values,9,0,2,7) = static_cast<REAL>(-7) / 2880 * dy;
        MAP(values,9,1,0,0) = static_cast<REAL>(-49) / 270 * dz;
        MAP(values,9,1,0,1) = static_cast<REAL>(-7) / 270 * dz;
        MAP(values,9,1,0,2) = static_cast<REAL>(-7) / 270 * dz;
        MAP(values,9,1,0,3) = static_cast<REAL>(-dz) / 270;
        MAP(values,9,1,0,4) = static_cast<REAL>(-49) / 270 * dz;
        MAP(values,9,1,0,5) = static_cast<REAL>(-7) / 270 * dz;
        MAP(values,9,1,0,6) = static_cast<REAL>(-7) / 270 * dz;
        MAP(values,9,1,0,7) = static_cast<REAL>(-dz) / 270;
        MAP(values,9,1,1,0) = static_cast<REAL>(-161) / 1350 * dx / dy * dz;
        MAP(values,9,1,1,1) = static_cast<REAL>(49) / 1350 * dx / dy * dz;
        MAP(values,9,1,1,2) = static_cast<REAL>(-23) / 1350 * dx / dy * dz;
        MAP(values,9,1,1,3) = static_cast<REAL>(7) / 1350 * dx / dy * dz;
        MAP(values,9,1,1,4) = static_cast<REAL>(-161) / 1350 * dx / dy * dz;
        MAP(values,9,1,1,5) = static_cast<REAL>(49) / 1350 * dx / dy * dz;
        MAP(values,9,1,1,6) = static_cast<REAL>(-23) / 1350 * dx / dy * dz;
        MAP(values,9,1,1,7) = static_cast<REAL>(7) / 1350 * dx / dy * dz;
        MAP(values,9,1,2,0) = static_cast<REAL>(-161) / 2880 * dx;
        MAP(values,9,1,2,1) = static_cast<REAL>(49) / 2880 * dx;
        MAP(values,9,1,2,2) = static_cast<REAL>(-23) / 2880 * dx;
        MAP(values,9,1,2,3) = static_cast<REAL>(7) / 2880 * dx;
        MAP(values,9,1,2,4) = static_cast<REAL>(161) / 2880 * dx;
        MAP(values,9,1,2,5) = static_cast<REAL>(-49) / 2880 * dx;
        MAP(values,9,1,2,6) = static_cast<REAL>(23) / 2880 * dx;
        MAP(values,9,1,2,7) = static_cast<REAL>(-7) / 2880 * dx;
        MAP(values,9,2,0,0) = static_cast<REAL>(161) / 2880 * dy;
        MAP(values,9,2,0,1) = static_cast<REAL>(23) / 2880 * dy;
        MAP(values,9,2,0,2) = static_cast<REAL>(-49) / 2880 * dy;
        MAP(values,9,2,0,3) = static_cast<REAL>(-7) / 2880 * dy;
        MAP(values,9,2,0,4) = static_cast<REAL>(-161) / 2880 * dy;
        MAP(values,9,2,0,5) = static_cast<REAL>(-23) / 2880 * dy;
        MAP(values,9,2,0,6) = static_cast<REAL>(49) / 2880 * dy;
        MAP(values,9,2,0,7) = static_cast<REAL>(7) / 2880 * dy;
        MAP(values,9,2,1,0) = static_cast<REAL>(161) / 2880 * dx;
        MAP(values,9,2,1,1) = static_cast<REAL>(-49) / 2880 * dx;
        MAP(values,9,2,1,2) = static_cast<REAL>(23) / 2880 * dx;
        MAP(values,9,2,1,3) = static_cast<REAL>(-7) / 2880 * dx;
        MAP(values,9,2,1,4) = static_cast<REAL>(-161) / 2880 * dx;
        MAP(values,9,2,1,5) = static_cast<REAL>(49) / 2880 * dx;
        MAP(values,9,2,1,6) = static_cast<REAL>(-23) / 2880 * dx;
        MAP(values,9,2,1,7) = static_cast<REAL>(7) / 2880 * dx;
        MAP(values,9,2,2,0) = static_cast<REAL>(529) / 10800 * dx * dy / dz;
        MAP(values,9,2,2,1) = static_cast<REAL>(-161) / 10800 * dx * dy / dz;
        MAP(values,9,2,2,2) = static_cast<REAL>(-161) / 10800 * dx * dy / dz;
        MAP(values,9,2,2,3) = static_cast<REAL>(49) / 10800 * dx * dy / dz;
        MAP(values,9,2,2,4) = static_cast<REAL>(529) / 10800 * dx * dy / dz;
        MAP(values,9,2,2,5) = static_cast<REAL>(-161) / 10800 * dx * dy / dz;
        MAP(values,9,2,2,6) = static_cast<REAL>(-161) / 10800 * dx * dy / dz;
        MAP(values,9,2,2,7) = static_cast<REAL>(49) / 10800 * dx * dy / dz;
        MAP(values,10,0,0,0) = static_cast<REAL>(92) / 675 / dx * dy * dz;
        MAP(values,10,0,0,1) = static_cast<REAL>(92) / 675 / dx * dy * dz;
        MAP(values,10,0,0,2) = static_cast<REAL>(-28) / 675 / dx * dy * dz;
        MAP(values,10,0,0,3) = static_cast<REAL>(-28) / 675 / dx * dy * dz;
        MAP(values,10,0,0,4) = static_cast<REAL>(92) / 675 / dx * dy * dz;
        MAP(values,10,0,0,5) = static_cast<REAL>(92) / 675 / dx * dy * dz;
        MAP(values,10,0,0,6) = static_cast<REAL>(-28) / 675 / dx * dy * dz;
        MAP(values,10,0,0,7) = static_cast<REAL>(-28) / 675 / dx * dy * dz;
        MAP(values,10,0,1,0) = static_cast<REAL>(7) / 45 * dz;
        MAP(values,10,0,1,1) = static_cast<REAL>(-7) / 45 * dz;
        MAP(values,10,0,1,2) = static_cast<REAL>(dz) / 45;
        MAP(values,10,0,1,3) = static_cast<REAL>(-dz) / 45;
        MAP(values,10,0,1,4) = static_cast<REAL>(7) / 45 * dz;
        MAP(values,10,0,1,5) = static_cast<REAL>(-7) / 45 * dz;
        MAP(values,10,0,1,6) = static_cast<REAL>(dz) / 45;
        MAP(values,10,0,1,7) = static_cast<REAL>(-dz) / 45;
        MAP(values,10,0,2,0) = static_cast<REAL>(23) / 480 * dy;
        MAP(values,10,0,2,1) = static_cast<REAL>(-23) / 480 * dy;
        MAP(values,10,0,2,2) = static_cast<REAL>(-7) / 480 * dy;
        MAP(values,10,0,2,3) = static_cast<REAL>(7) / 480 * dy;
        MAP(values,10,0,2,4) = static_cast<REAL>(-23) / 480 * dy;
        MAP(values,10,0,2,5) = static_cast<REAL>(23) / 480 * dy;
        MAP(values,10,0,2,6) = static_cast<REAL>(7) / 480 * dy;
        MAP(values,10,0,2,7) = static_cast<REAL>(-7) / 480 * dy;
        MAP(values,10,1,0,0) = static_cast<REAL>(-7) / 45 * dz;
        MAP(values,10,1,0,1) = static_cast<REAL>(7) / 45 * dz;
        MAP(values,10,1,0,2) = static_cast<REAL>(-dz) / 45;
        MAP(values,10,1,0,3) = static_cast<REAL>(dz) / 45;
        MAP(values,10,1,0,4) = static_cast<REAL>(-7) / 45 * dz;
        MAP(values,10,1,0,5) = static_cast<REAL>(7) / 45 * dz;
        MAP(values,10,1,0,6) = static_cast<REAL>(-dz) / 45;
        MAP(values,10,1,0,7) = static_cast<REAL>(dz) / 45;
        MAP(values,10,1,1,0) = static_cast<REAL>(-224) / 675 * dx / dy * dz;
        MAP(values,10,1,1,1) = static_cast<REAL>(-224) / 675 * dx / dy * dz;
        MAP(values,10,1,1,2) = static_cast<REAL>(-32) / 675 * dx / dy * dz;
        MAP(values,10,1,1,3) = static_cast<REAL>(-32) / 675 * dx / dy * dz;
        MAP(values,10,1,1,4) = static_cast<REAL>(-224) / 675 * dx / dy * dz;
        MAP(values,10,1,1,5) = static_cast<REAL>(-224) / 675 * dx / dy * dz;
        MAP(values,10,1,1,6) = static_cast<REAL>(-32) / 675 * dx / dy * dz;
        MAP(values,10,1,1,7) = static_cast<REAL>(-32) / 675 * dx / dy * dz;
        MAP(values,10,1,2,0) = static_cast<REAL>(-7) / 45 * dx;
        MAP(values,10,1,2,1) = static_cast<REAL>(-7) / 45 * dx;
        MAP(values,10,1,2,2) = static_cast<REAL>(-dx) / 45;
        MAP(values,10,1,2,3) = static_cast<REAL>(-dx) / 45;
        MAP(values,10,1,2,4) = static_cast<REAL>(7) / 45 * dx;
        MAP(values,10,1,2,5) = static_cast<REAL>(7) / 45 * dx;
        MAP(values,10,1,2,6) = static_cast<REAL>(dx) / 45;
        MAP(values,10,1,2,7) = static_cast<REAL>(dx) / 45;
        MAP(values,10,2,0,0) = static_cast<REAL>(23) / 480 * dy;
        MAP(values,10,2,0,1) = static_cast<REAL>(-23) / 480 * dy;
        MAP(values,10,2,0,2) = static_cast<REAL>(-7) / 480 * dy;
        MAP(values,10,2,0,3) = static_cast<REAL>(7) / 480 * dy;
        MAP(values,10,2,0,4) = static_cast<REAL>(-23) / 480 * dy;
        MAP(values,10,2,0,5) = static_cast<REAL>(23) / 480 * dy;
        MAP(values,10,2,0,6) = static_cast<REAL>(7) / 480 * dy;
        MAP(values,10,2,0,7) = static_cast<REAL>(-7) / 480 * dy;
        MAP(values,10,2,1,0) = static_cast<REAL>(7) / 45 * dx;
        MAP(values,10,2,1,1) = static_cast<REAL>(7) / 45 * dx;
        MAP(values,10,2,1,2) = static_cast<REAL>(dx) / 45;
        MAP(values,10,2,1,3) = static_cast<REAL>(dx) / 45;
        MAP(values,10,2,1,4) = static_cast<REAL>(-7) / 45 * dx;
        MAP(values,10,2,1,5) = static_cast<REAL>(-7) / 45 * dx;
        MAP(values,10,2,1,6) = static_cast<REAL>(-dx) / 45;
        MAP(values,10,2,1,7) = static_cast<REAL>(-dx) / 45;
        MAP(values,10,2,2,0) = static_cast<REAL>(92) / 675 * dx * dy / dz;
        MAP(values,10,2,2,1) = static_cast<REAL>(92) / 675 * dx * dy / dz;
        MAP(values,10,2,2,2) = static_cast<REAL>(-28) / 675 * dx * dy / dz;
        MAP(values,10,2,2,3) = static_cast<REAL>(-28) / 675 * dx * dy / dz;
        MAP(values,10,2,2,4) = static_cast<REAL>(92) / 675 * dx * dy / dz;
        MAP(values,10,2,2,5) = static_cast<REAL>(92) / 675 * dx * dy / dz;
        MAP(values,10,2,2,6) = static_cast<REAL>(-28) / 675 * dx * dy / dz;
        MAP(values,10,2,2,7) = static_cast<REAL>(-28) / 675 * dx * dy / dz;
        MAP(values,11,0,0,0) = static_cast<REAL>(-23) / 1350 / dx * dy * dz;
        MAP(values,11,0,0,1) = static_cast<REAL>(-161) / 1350 / dx * dy * dz;
        MAP(values,11,0,0,2) = static_cast<REAL>(7) / 1350 / dx * dy * dz;
        MAP(values,11,0,0,3) = static_cast<REAL>(49) / 1350 / dx * dy * dz;
        MAP(values,11,0,0,4) = static_cast<REAL>(-23) / 1350 / dx * dy * dz;
        MAP(values,11,0,0,5) = static_cast<REAL>(-161) / 1350 / dx * dy * dz;
        MAP(values,11,0,0,6) = static_cast<REAL>(7) / 1350 / dx * dy * dz;
        MAP(values,11,0,0,7) = static_cast<REAL>(49) / 1350 / dx * dy * dz;
        MAP(values,11,0,1,0) = static_cast<REAL>(7) / 270 * dz;
        MAP(values,11,0,1,1) = static_cast<REAL>(49) / 270 * dz;
        MAP(values,11,0,1,2) = static_cast<REAL>(dz) / 270;
        MAP(values,11,0,1,3) = static_cast<REAL>(7) / 270 * dz;
        MAP(values,11,0,1,4) = static_cast<REAL>(7) / 270 * dz;
        MAP(values,11,0,1,5) = static_cast<REAL>(49) / 270 * dz;
        MAP(values,11,0,1,6) = static_cast<REAL>(dz) / 270;
        MAP(values,11,0,1,7) = static_cast<REAL>(7) / 270 * dz;
        MAP(values,11,0,2,0) = static_cast<REAL>(23) / 2880 * dy;
        MAP(values,11,0,2,1) = static_cast<REAL>(161) / 2880 * dy;
        MAP(values,11,0,2,2) = static_cast<REAL>(-7) / 2880 * dy;
        MAP(values,11,0,2,3) = static_cast<REAL>(-49) / 2880 * dy;
        MAP(values,11,0,2,4) = static_cast<REAL>(-23) / 2880 * dy;
        MAP(values,11,0,2,5) = static_cast<REAL>(-161) / 2880 * dy;
        MAP(values,11,0,2,6) = static_cast<REAL>(7) / 2880 * dy;
        MAP(values,11,0,2,7) = static_cast<REAL>(49) / 2880 * dy;
        MAP(values,11,1,0,0) = static_cast<REAL>(7) / 270 * dz;
        MAP(values,11,1,0,1) = static_cast<REAL>(49) / 270 * dz;
        MAP(values,11,1,0,2) = static_cast<REAL>(dz) / 270;
        MAP(values,11,1,0,3) = static_cast<REAL>(7) / 270 * dz;
        MAP(values,11,1,0,4) = static_cast<REAL>(7) / 270 * dz;
        MAP(values,11,1,0,5) = static_cast<REAL>(49) / 270 * dz;
        MAP(values,11,1,0,6) = static_cast<REAL>(dz) / 270;
        MAP(values,11,1,0,7) = static_cast<REAL>(7) / 270 * dz;
        MAP(values,11,1,1,0) = static_cast<REAL>(49) / 1350 * dx / dy * dz;
        MAP(values,11,1,1,1) = static_cast<REAL>(-161) / 1350 * dx / dy * dz;
        MAP(values,11,1,1,2) = static_cast<REAL>(7) / 1350 * dx / dy * dz;
        MAP(values,11,1,1,3) = static_cast<REAL>(-23) / 1350 * dx / dy * dz;
        MAP(values,11,1,1,4) = static_cast<REAL>(49) / 1350 * dx / dy * dz;
        MAP(values,11,1,1,5) = static_cast<REAL>(-161) / 1350 * dx / dy * dz;
        MAP(values,11,1,1,6) = static_cast<REAL>(7) / 1350 * dx / dy * dz;
        MAP(values,11,1,1,7) = static_cast<REAL>(-23) / 1350 * dx / dy * dz;
        MAP(values,11,1,2,0) = static_cast<REAL>(49) / 2880 * dx;
        MAP(values,11,1,2,1) = static_cast<REAL>(-161) / 2880 * dx;
        MAP(values,11,1,2,2) = static_cast<REAL>(7) / 2880 * dx;
        MAP(values,11,1,2,3) = static_cast<REAL>(-23) / 2880 * dx;
        MAP(values,11,1,2,4) = static_cast<REAL>(-49) / 2880 * dx;
        MAP(values,11,1,2,5) = static_cast<REAL>(161) / 2880 * dx;
        MAP(values,11,1,2,6) = static_cast<REAL>(-7) / 2880 * dx;
        MAP(values,11,1,2,7) = static_cast<REAL>(23) / 2880 * dx;
        MAP(values,11,2,0,0) = static_cast<REAL>(-23) / 2880 * dy;
        MAP(values,11,2,0,1) = static_cast<REAL>(-161) / 2880 * dy;
        MAP(values,11,2,0,2) = static_cast<REAL>(7) / 2880 * dy;
        MAP(values,11,2,0,3) = static_cast<REAL>(49) / 2880 * dy;
        MAP(values,11,2,0,4) = static_cast<REAL>(23) / 2880 * dy;
        MAP(values,11,2,0,5) = static_cast<REAL>(161) / 2880 * dy;
        MAP(values,11,2,0,6) = static_cast<REAL>(-7) / 2880 * dy;
        MAP(values,11,2,0,7) = static_cast<REAL>(-49) / 2880 * dy;
        MAP(values,11,2,1,0) = static_cast<REAL>(-49) / 2880 * dx;
        MAP(values,11,2,1,1) = static_cast<REAL>(161) / 2880 * dx;
        MAP(values,11,2,1,2) = static_cast<REAL>(-7) / 2880 * dx;
        MAP(values,11,2,1,3) = static_cast<REAL>(23) / 2880 * dx;
        MAP(values,11,2,1,4) = static_cast<REAL>(49) / 2880 * dx;
        MAP(values,11,2,1,5) = static_cast<REAL>(-161) / 2880 * dx;
        MAP(values,11,2,1,6) = static_cast<REAL>(7) / 2880 * dx;
        MAP(values,11,2,1,7) = static_cast<REAL>(-23) / 2880 * dx;
        MAP(values,11,2,2,0) = static_cast<REAL>(-161) / 10800 * dx * dy / dz;
        MAP(values,11,2,2,1) = static_cast<REAL>(529) / 10800 * dx * dy / dz;
        MAP(values,11,2,2,2) = static_cast<REAL>(49) / 10800 * dx * dy / dz;
        MAP(values,11,2,2,3) = static_cast<REAL>(-161) / 10800 * dx * dy / dz;
        MAP(values,11,2,2,4) = static_cast<REAL>(-161) / 10800 * dx * dy / dz;
        MAP(values,11,2,2,5) = static_cast<REAL>(529) / 10800 * dx * dy / dz;
        MAP(values,11,2,2,6) = static_cast<REAL>(49) / 10800 * dx * dy / dz;
        MAP(values,11,2,2,7) = static_cast<REAL>(-161) / 10800 * dx * dy / dz;
        MAP(values,12,0,0,0) = static_cast<REAL>(-224) / 675 / dx * dy * dz;
        MAP(values,12,0,0,1) = static_cast<REAL>(-32) / 675 / dx * dy * dz;
        MAP(values,12,0,0,2) = static_cast<REAL>(-224) / 675 / dx * dy * dz;
        MAP(values,12,0,0,3) = static_cast<REAL>(-32) / 675 / dx * dy * dz;
        MAP(values,12,0,0,4) = static_cast<REAL>(-224) / 675 / dx * dy * dz;
        MAP(values,12,0,0,5) = static_cast<REAL>(-32) / 675 / dx * dy * dz;
        MAP(values,12,0,0,6) = static_cast<REAL>(-224) / 675 / dx * dy * dz;
        MAP(values,12,0,0,7) = static_cast<REAL>(-32) / 675 / dx * dy * dz;
        MAP(values,12,0,1,0) = static_cast<REAL>(-7) / 45 * dz;
        MAP(values,12,0,1,1) = static_cast<REAL>(-dz) / 45;
        MAP(values,12,0,1,2) = static_cast<REAL>(7) / 45 * dz;
        MAP(values,12,0,1,3) = static_cast<REAL>(dz) / 45;
        MAP(values,12,0,1,4) = static_cast<REAL>(-7) / 45 * dz;
        MAP(values,12,0,1,5) = static_cast<REAL>(-dz) / 45;
        MAP(values,12,0,1,6) = static_cast<REAL>(7) / 45 * dz;
        MAP(values,12,0,1,7) = static_cast<REAL>(dz) / 45;
        MAP(values,12,0,2,0) = static_cast<REAL>(-7) / 45 * dy;
        MAP(values,12,0,2,1) = static_cast<REAL>(-dy) / 45;
        MAP(values,12,0,2,2) = static_cast<REAL>(-7) / 45 * dy;
        MAP(values,12,0,2,3) = static_cast<REAL>(-dy) / 45;
        MAP(values,12,0,2,4) = static_cast<REAL>(7) / 45 * dy;
        MAP(values,12,0,2,5) = static_cast<REAL>(dy) / 45;
        MAP(values,12,0,2,6) = static_cast<REAL>(7) / 45 * dy;
        MAP(values,12,0,2,7) = static_cast<REAL>(dy) / 45;
        MAP(values,12,1,0,0) = static_cast<REAL>(7) / 45 * dz;
        MAP(values,12,1,0,1) = static_cast<REAL>(dz) / 45;
        MAP(values,12,1,0,2) = static_cast<REAL>(-7) / 45 * dz;
        MAP(values,12,1,0,3) = static_cast<REAL>(-dz) / 45;
        MAP(values,12,1,0,4) = static_cast<REAL>(7) / 45 * dz;
        MAP(values,12,1,0,5) = static_cast<REAL>(dz) / 45;
        MAP(values,12,1,0,6) = static_cast<REAL>(-7) / 45 * dz;
        MAP(values,12,1,0,7) = static_cast<REAL>(-dz) / 45;
        MAP(values,12,1,1,0) = static_cast<REAL>(92) / 675 * dx / dy * dz;
        MAP(values,12,1,1,1) = static_cast<REAL>(-28) / 675 * dx / dy * dz;
        MAP(values,12,1,1,2) = static_cast<REAL>(92) / 675 * dx / dy * dz;
        MAP(values,12,1,1,3) = static_cast<REAL>(-28) / 675 * dx / dy * dz;
        MAP(values,12,1,1,4) = static_cast<REAL>(92) / 675 * dx / dy * dz;
        MAP(values,12,1,1,5) = static_cast<REAL>(-28) / 675 * dx / dy * dz;
        MAP(values,12,1,1,6) = static_cast<REAL>(92) / 675 * dx / dy * dz;
        MAP(values,12,1,1,7) = static_cast<REAL>(-28) / 675 * dx / dy * dz;
        MAP(values,12,1,2,0) = static_cast<REAL>(23) / 480 * dx;
        MAP(values,12,1,2,1) = static_cast<REAL>(-7) / 480 * dx;
        MAP(values,12,1,2,2) = static_cast<REAL>(-23) / 480 * dx;
        MAP(values,12,1,2,3) = static_cast<REAL>(7) / 480 * dx;
        MAP(values,12,1,2,4) = static_cast<REAL>(-23) / 480 * dx;
        MAP(values,12,1,2,5) = static_cast<REAL>(7) / 480 * dx;
        MAP(values,12,1,2,6) = static_cast<REAL>(23) / 480 * dx;
        MAP(values,12,1,2,7) = static_cast<REAL>(-7) / 480 * dx;
        MAP(values,12,2,0,0) = static_cast<REAL>(7) / 45 * dy;
        MAP(values,12,2,0,1) = static_cast<REAL>(dy) / 45;
        MAP(values,12,2,0,2) = static_cast<REAL>(7) / 45 * dy;
        MAP(values,12,2,0,3) = static_cast<REAL>(dy) / 45;
        MAP(values,12,2,0,4) = static_cast<REAL>(-7) / 45 * dy;
        MAP(values,12,2,0,5) = static_cast<REAL>(-dy) / 45;
        MAP(values,12,2,0,6) = static_cast<REAL>(-7) / 45 * dy;
        MAP(values,12,2,0,7) = static_cast<REAL>(-dy) / 45;
        MAP(values,12,2,1,0) = static_cast<REAL>(23) / 480 * dx;
        MAP(values,12,2,1,1) = static_cast<REAL>(-7) / 480 * dx;
        MAP(values,12,2,1,2) = static_cast<REAL>(-23) / 480 * dx;
        MAP(values,12,2,1,3) = static_cast<REAL>(7) / 480 * dx;
        MAP(values,12,2,1,4) = static_cast<REAL>(-23) / 480 * dx;
        MAP(values,12,2,1,5) = static_cast<REAL>(7) / 480 * dx;
        MAP(values,12,2,1,6) = static_cast<REAL>(23) / 480 * dx;
        MAP(values,12,2,1,7) = static_cast<REAL>(-7) / 480 * dx;
        MAP(values,12,2,2,0) = static_cast<REAL>(92) / 675 * dx * dy / dz;
        MAP(values,12,2,2,1) = static_cast<REAL>(-28) / 675 * dx * dy / dz;
        MAP(values,12,2,2,2) = static_cast<REAL>(92) / 675 * dx * dy / dz;
        MAP(values,12,2,2,3) = static_cast<REAL>(-28) / 675 * dx * dy / dz;
        MAP(values,12,2,2,4) = static_cast<REAL>(92) / 675 * dx * dy / dz;
        MAP(values,12,2,2,5) = static_cast<REAL>(-28) / 675 * dx * dy / dz;
        MAP(values,12,2,2,6) = static_cast<REAL>(92) / 675 * dx * dy / dz;
        MAP(values,12,2,2,7) = static_cast<REAL>(-28) / 675 * dx * dy / dz;
        MAP(values,13,0,0,0) = static_cast<REAL>(256) / 675 / dx * dy * dz;
        MAP(values,13,0,0,1) = static_cast<REAL>(256) / 675 / dx * dy * dz;
        MAP(values,13,0,0,2) = static_cast<REAL>(256) / 675 / dx * dy * dz;
        MAP(values,13,0,0,3) = static_cast<REAL>(256) / 675 / dx * dy * dz;
        MAP(values,13,0,0,4) = static_cast<REAL>(256) / 675 / dx * dy * dz;
        MAP(values,13,0,0,5) = static_cast<REAL>(256) / 675 / dx * dy * dz;
        MAP(values,13,0,0,6) = static_cast<REAL>(256) / 675 / dx * dy * dz;
        MAP(values,13,0,0,7) = static_cast<REAL>(256) / 675 / dx * dy * dz;
        MAP(values,13,0,1,0) = static_cast<REAL>(2) / 15 * dz;
        MAP(values,13,0,1,1) = static_cast<REAL>(-2) / 15 * dz;
        MAP(values,13,0,1,2) = static_cast<REAL>(-2) / 15 * dz;
        MAP(values,13,0,1,3) = static_cast<REAL>(2) / 15 * dz;
        MAP(values,13,0,1,4) = static_cast<REAL>(2) / 15 * dz;
        MAP(values,13,0,1,5) = static_cast<REAL>(-2) / 15 * dz;
        MAP(values,13,0,1,6) = static_cast<REAL>(-2) / 15 * dz;
        MAP(values,13,0,1,7) = static_cast<REAL>(2) / 15 * dz;
        MAP(values,13,0,2,0) = static_cast<REAL>(2) / 15 * dy;
        MAP(values,13,0,2,1) = static_cast<REAL>(-2) / 15 * dy;
        MAP(values,13,0,2,2) = static_cast<REAL>(2) / 15 * dy;
        MAP(values,13,0,2,3) = static_cast<REAL>(-2) / 15 * dy;
        MAP(values,13,0,2,4) = static_cast<REAL>(-2) / 15 * dy;
        MAP(values,13,0,2,5) = static_cast<REAL>(2) / 15 * dy;
        MAP(values,13,0,2,6) = static_cast<REAL>(-2) / 15 * dy;
        MAP(values,13,0,2,7) = static_cast<REAL>(2) / 15 * dy;
        MAP(values,13,1,0,0) = static_cast<REAL>(2) / 15 * dz;
        MAP(values,13,1,0,1) = static_cast<REAL>(-2) / 15 * dz;
        MAP(values,13,1,0,2) = static_cast<REAL>(-2) / 15 * dz;
        MAP(values,13,1,0,3) = static_cast<REAL>(2) / 15 * dz;
        MAP(values,13,1,0,4) = static_cast<REAL>(2) / 15 * dz;
        MAP(values,13,1,0,5) = static_cast<REAL>(-2) / 15 * dz;
        MAP(values,13,1,0,6) = static_cast<REAL>(-2) / 15 * dz;
        MAP(values,13,1,0,7) = static_cast<REAL>(2) / 15 * dz;
        MAP(values,13,1,1,0) = static_cast<REAL>(256) / 675 * dx / dy * dz;
        MAP(values,13,1,1,1) = static_cast<REAL>(256) / 675 * dx / dy * dz;
        MAP(values,13,1,1,2) = static_cast<REAL>(256) / 675 * dx / dy * dz;
        MAP(values,13,1,1,3) = static_cast<REAL>(256) / 675 * dx / dy * dz;
        MAP(values,13,1,1,4) = static_cast<REAL>(256) / 675 * dx / dy * dz;
        MAP(values,13,1,1,5) = static_cast<REAL>(256) / 675 * dx / dy * dz;
        MAP(values,13,1,1,6) = static_cast<REAL>(256) / 675 * dx / dy * dz;
        MAP(values,13,1,1,7) = static_cast<REAL>(256) / 675 * dx / dy * dz;
        MAP(values,13,1,2,0) = static_cast<REAL>(2) / 15 * dx;
        MAP(values,13,1,2,1) = static_cast<REAL>(2) / 15 * dx;
        MAP(values,13,1,2,2) = static_cast<REAL>(-2) / 15 * dx;
        MAP(values,13,1,2,3) = static_cast<REAL>(-2) / 15 * dx;
        MAP(values,13,1,2,4) = static_cast<REAL>(-2) / 15 * dx;
        MAP(values,13,1,2,5) = static_cast<REAL>(-2) / 15 * dx;
        MAP(values,13,1,2,6) = static_cast<REAL>(2) / 15 * dx;
        MAP(values,13,1,2,7) = static_cast<REAL>(2) / 15 * dx;
        MAP(values,13,2,0,0) = static_cast<REAL>(2) / 15 * dy;
        MAP(values,13,2,0,1) = static_cast<REAL>(-2) / 15 * dy;
        MAP(values,13,2,0,2) = static_cast<REAL>(2) / 15 * dy;
        MAP(values,13,2,0,3) = static_cast<REAL>(-2) / 15 * dy;
        MAP(values,13,2,0,4) = static_cast<REAL>(-2) / 15 * dy;
        MAP(values,13,2,0,5) = static_cast<REAL>(2) / 15 * dy;
        MAP(values,13,2,0,6) = static_cast<REAL>(-2) / 15 * dy;
        MAP(values,13,2,0,7) = static_cast<REAL>(2) / 15 * dy;
        MAP(values,13,2,1,0) = static_cast<REAL>(2) / 15 * dx;
        MAP(values,13,2,1,1) = static_cast<REAL>(2) / 15 * dx;
        MAP(values,13,2,1,2) = static_cast<REAL>(-2) / 15 * dx;
        MAP(values,13,2,1,3) = static_cast<REAL>(-2) / 15 * dx;
        MAP(values,13,2,1,4) = static_cast<REAL>(-2) / 15 * dx;
        MAP(values,13,2,1,5) = static_cast<REAL>(-2) / 15 * dx;
        MAP(values,13,2,1,6) = static_cast<REAL>(2) / 15 * dx;
        MAP(values,13,2,1,7) = static_cast<REAL>(2) / 15 * dx;
        MAP(values,13,2,2,0) = static_cast<REAL>(256) / 675 * dx * dy / dz;
        MAP(values,13,2,2,1) = static_cast<REAL>(256) / 675 * dx * dy / dz;
        MAP(values,13,2,2,2) = static_cast<REAL>(256) / 675 * dx * dy / dz;
        MAP(values,13,2,2,3) = static_cast<REAL>(256) / 675 * dx * dy / dz;
        MAP(values,13,2,2,4) = static_cast<REAL>(256) / 675 * dx * dy / dz;
        MAP(values,13,2,2,5) = static_cast<REAL>(256) / 675 * dx * dy / dz;
        MAP(values,13,2,2,6) = static_cast<REAL>(256) / 675 * dx * dy / dz;
        MAP(values,13,2,2,7) = static_cast<REAL>(256) / 675 * dx * dy / dz;
        MAP(values,14,0,0,0) = static_cast<REAL>(-32) / 675 / dx * dy * dz;
        MAP(values,14,0,0,1) = static_cast<REAL>(-224) / 675 / dx * dy * dz;
        MAP(values,14,0,0,2) = static_cast<REAL>(-32) / 675 / dx * dy * dz;
        MAP(values,14,0,0,3) = static_cast<REAL>(-224) / 675 / dx * dy * dz;
        MAP(values,14,0,0,4) = static_cast<REAL>(-32) / 675 / dx * dy * dz;
        MAP(values,14,0,0,5) = static_cast<REAL>(-224) / 675 / dx * dy * dz;
        MAP(values,14,0,0,6) = static_cast<REAL>(-32) / 675 / dx * dy * dz;
        MAP(values,14,0,0,7) = static_cast<REAL>(-224) / 675 / dx * dy * dz;
        MAP(values,14,0,1,0) = static_cast<REAL>(dz) / 45;
        MAP(values,14,0,1,1) = static_cast<REAL>(7) / 45 * dz;
        MAP(values,14,0,1,2) = static_cast<REAL>(-dz) / 45;
        MAP(values,14,0,1,3) = static_cast<REAL>(-7) / 45 * dz;
        MAP(values,14,0,1,4) = static_cast<REAL>(dz) / 45;
        MAP(values,14,0,1,5) = static_cast<REAL>(7) / 45 * dz;
        MAP(values,14,0,1,6) = static_cast<REAL>(-dz) / 45;
        MAP(values,14,0,1,7) = static_cast<REAL>(-7) / 45 * dz;
        MAP(values,14,0,2,0) = static_cast<REAL>(dy) / 45;
        MAP(values,14,0,2,1) = static_cast<REAL>(7) / 45 * dy;
        MAP(values,14,0,2,2) = static_cast<REAL>(dy) / 45;
        MAP(values,14,0,2,3) = static_cast<REAL>(7) / 45 * dy;
        MAP(values,14,0,2,4) = static_cast<REAL>(-dy) / 45;
        MAP(values,14,0,2,5) = static_cast<REAL>(-7) / 45 * dy;
        MAP(values,14,0,2,6) = static_cast<REAL>(-dy) / 45;
        MAP(values,14,0,2,7) = static_cast<REAL>(-7) / 45 * dy;
        MAP(values,14,1,0,0) = static_cast<REAL>(-dz) / 45;
        MAP(values,14,1,0,1) = static_cast<REAL>(-7) / 45 * dz;
        MAP(values,14,1,0,2) = static_cast<REAL>(dz) / 45;
        MAP(values,14,1,0,3) = static_cast<REAL>(7) / 45 * dz;
        MAP(values,14,1,0,4) = static_cast<REAL>(-dz) / 45;
        MAP(values,14,1,0,5) = static_cast<REAL>(-7) / 45 * dz;
        MAP(values,14,1,0,6) = static_cast<REAL>(dz) / 45;
        MAP(values,14,1,0,7) = static_cast<REAL>(7) / 45 * dz;
        MAP(values,14,1,1,0) = static_cast<REAL>(-28) / 675 * dx / dy * dz;
        MAP(values,14,1,1,1) = static_cast<REAL>(92) / 675 * dx / dy * dz;
        MAP(values,14,1,1,2) = static_cast<REAL>(-28) / 675 * dx / dy * dz;
        MAP(values,14,1,1,3) = static_cast<REAL>(92) / 675 * dx / dy * dz;
        MAP(values,14,1,1,4) = static_cast<REAL>(-28) / 675 * dx / dy * dz;
        MAP(values,14,1,1,5) = static_cast<REAL>(92) / 675 * dx / dy * dz;
        MAP(values,14,1,1,6) = static_cast<REAL>(-28) / 675 * dx / dy * dz;
        MAP(values,14,1,1,7) = static_cast<REAL>(92) / 675 * dx / dy * dz;
        MAP(values,14,1,2,0) = static_cast<REAL>(-7) / 480 * dx;
        MAP(values,14,1,2,1) = static_cast<REAL>(23) / 480 * dx;
        MAP(values,14,1,2,2) = static_cast<REAL>(7) / 480 * dx;
        MAP(values,14,1,2,3) = static_cast<REAL>(-23) / 480 * dx;
        MAP(values,14,1,2,4) = static_cast<REAL>(7) / 480 * dx;
        MAP(values,14,1,2,5) = static_cast<REAL>(-23) / 480 * dx;
        MAP(values,14,1,2,6) = static_cast<REAL>(-7) / 480 * dx;
        MAP(values,14,1,2,7) = static_cast<REAL>(23) / 480 * dx;
        MAP(values,14,2,0,0) = static_cast<REAL>(-dy) / 45;
        MAP(values,14,2,0,1) = static_cast<REAL>(-7) / 45 * dy;
        MAP(values,14,2,0,2) = static_cast<REAL>(-dy) / 45;
        MAP(values,14,2,0,3) = static_cast<REAL>(-7) / 45 * dy;
        MAP(values,14,2,0,4) = static_cast<REAL>(dy) / 45;
        MAP(values,14,2,0,5) = static_cast<REAL>(7) / 45 * dy;
        MAP(values,14,2,0,6) = static_cast<REAL>(dy) / 45;
        MAP(values,14,2,0,7) = static_cast<REAL>(7) / 45 * dy;
        MAP(values,14,2,1,0) = static_cast<REAL>(-7) / 480 * dx;
        MAP(values,14,2,1,1) = static_cast<REAL>(23) / 480 * dx;
        MAP(values,14,2,1,2) = static_cast<REAL>(7) / 480 * dx;
        MAP(values,14,2,1,3) = static_cast<REAL>(-23) / 480 * dx;
        MAP(values,14,2,1,4) = static_cast<REAL>(7) / 480 * dx;
        MAP(values,14,2,1,5) = static_cast<REAL>(-23) / 480 * dx;
        MAP(values,14,2,1,6) = static_cast<REAL>(-7) / 480 * dx;
        MAP(values,14,2,1,7) = static_cast<REAL>(23) / 480 * dx;
        MAP(values,14,2,2,0) = static_cast<REAL>(-28) / 675 * dx * dy / dz;
        MAP(values,14,2,2,1) = static_cast<REAL>(92) / 675 * dx * dy / dz;
        MAP(values,14,2,2,2) = static_cast<REAL>(-28) / 675 * dx * dy / dz;
        MAP(values,14,2,2,3) = static_cast<REAL>(92) / 675 * dx * dy / dz;
        MAP(values,14,2,2,4) = static_cast<REAL>(-28) / 675 * dx * dy / dz;
        MAP(values,14,2,2,5) = static_cast<REAL>(92) / 675 * dx * dy / dz;
        MAP(values,14,2,2,6) = static_cast<REAL>(-28) / 675 * dx * dy / dz;
        MAP(values,14,2,2,7) = static_cast<REAL>(92) / 675 * dx * dy / dz;
        MAP(values,15,0,0,0) = static_cast<REAL>(49) / 1350 / dx * dy * dz;
        MAP(values,15,0,0,1) = static_cast<REAL>(7) / 1350 / dx * dy * dz;
        MAP(values,15,0,0,2) = static_cast<REAL>(-161) / 1350 / dx * dy * dz;
        MAP(values,15,0,0,3) = static_cast<REAL>(-23) / 1350 / dx * dy * dz;
        MAP(values,15,0,0,4) = static_cast<REAL>(49) / 1350 / dx * dy * dz;
        MAP(values,15,0,0,5) = static_cast<REAL>(7) / 1350 / dx * dy * dz;
        MAP(values,15,0,0,6) = static_cast<REAL>(-161) / 1350 / dx * dy * dz;
        MAP(values,15,0,0,7) = static_cast<REAL>(-23) / 1350 / dx * dy * dz;
        MAP(values,15,0,1,0) = static_cast<REAL>(7) / 270 * dz;
        MAP(values,15,0,1,1) = static_cast<REAL>(dz) / 270;
        MAP(values,15,0,1,2) = static_cast<REAL>(49) / 270 * dz;
        MAP(values,15,0,1,3) = static_cast<REAL>(7) / 270 * dz;
        MAP(values,15,0,1,4) = static_cast<REAL>(7) / 270 * dz;
        MAP(values,15,0,1,5) = static_cast<REAL>(dz) / 270;
        MAP(values,15,0,1,6) = static_cast<REAL>(49) / 270 * dz;
        MAP(values,15,0,1,7) = static_cast<REAL>(7) / 270 * dz;
        MAP(values,15,0,2,0) = static_cast<REAL>(49) / 2880 * dy;
        MAP(values,15,0,2,1) = static_cast<REAL>(7) / 2880 * dy;
        MAP(values,15,0,2,2) = static_cast<REAL>(-161) / 2880 * dy;
        MAP(values,15,0,2,3) = static_cast<REAL>(-23) / 2880 * dy;
        MAP(values,15,0,2,4) = static_cast<REAL>(-49) / 2880 * dy;
        MAP(values,15,0,2,5) = static_cast<REAL>(-7) / 2880 * dy;
        MAP(values,15,0,2,6) = static_cast<REAL>(161) / 2880 * dy;
        MAP(values,15,0,2,7) = static_cast<REAL>(23) / 2880 * dy;
        MAP(values,15,1,0,0) = static_cast<REAL>(7) / 270 * dz;
        MAP(values,15,1,0,1) = static_cast<REAL>(dz) / 270;
        MAP(values,15,1,0,2) = static_cast<REAL>(49) / 270 * dz;
        MAP(values,15,1,0,3) = static_cast<REAL>(7) / 270 * dz;
        MAP(values,15,1,0,4) = static_cast<REAL>(7) / 270 * dz;
        MAP(values,15,1,0,5) = static_cast<REAL>(dz) / 270;
        MAP(values,15,1,0,6) = static_cast<REAL>(49) / 270 * dz;
        MAP(values,15,1,0,7) = static_cast<REAL>(7) / 270 * dz;
        MAP(values,15,1,1,0) = static_cast<REAL>(-23) / 1350 * dx / dy * dz;
        MAP(values,15,1,1,1) = static_cast<REAL>(7) / 1350 * dx / dy * dz;
        MAP(values,15,1,1,2) = static_cast<REAL>(-161) / 1350 * dx / dy * dz;
        MAP(values,15,1,1,3) = static_cast<REAL>(49) / 1350 * dx / dy * dz;
        MAP(values,15,1,1,4) = static_cast<REAL>(-23) / 1350 * dx / dy * dz;
        MAP(values,15,1,1,5) = static_cast<REAL>(7) / 1350 * dx / dy * dz;
        MAP(values,15,1,1,6) = static_cast<REAL>(-161) / 1350 * dx / dy * dz;
        MAP(values,15,1,1,7) = static_cast<REAL>(49) / 1350 * dx / dy * dz;
        MAP(values,15,1,2,0) = static_cast<REAL>(23) / 2880 * dx;
        MAP(values,15,1,2,1) = static_cast<REAL>(-7) / 2880 * dx;
        MAP(values,15,1,2,2) = static_cast<REAL>(161) / 2880 * dx;
        MAP(values,15,1,2,3) = static_cast<REAL>(-49) / 2880 * dx;
        MAP(values,15,1,2,4) = static_cast<REAL>(-23) / 2880 * dx;
        MAP(values,15,1,2,5) = static_cast<REAL>(7) / 2880 * dx;
        MAP(values,15,1,2,6) = static_cast<REAL>(-161) / 2880 * dx;
        MAP(values,15,1,2,7) = static_cast<REAL>(49) / 2880 * dx;
        MAP(values,15,2,0,0) = static_cast<REAL>(-49) / 2880 * dy;
        MAP(values,15,2,0,1) = static_cast<REAL>(-7) / 2880 * dy;
        MAP(values,15,2,0,2) = static_cast<REAL>(161) / 2880 * dy;
        MAP(values,15,2,0,3) = static_cast<REAL>(23) / 2880 * dy;
        MAP(values,15,2,0,4) = static_cast<REAL>(49) / 2880 * dy;
        MAP(values,15,2,0,5) = static_cast<REAL>(7) / 2880 * dy;
        MAP(values,15,2,0,6) = static_cast<REAL>(-161) / 2880 * dy;
        MAP(values,15,2,0,7) = static_cast<REAL>(-23) / 2880 * dy;
        MAP(values,15,2,1,0) = static_cast<REAL>(-23) / 2880 * dx;
        MAP(values,15,2,1,1) = static_cast<REAL>(7) / 2880 * dx;
        MAP(values,15,2,1,2) = static_cast<REAL>(-161) / 2880 * dx;
        MAP(values,15,2,1,3) = static_cast<REAL>(49) / 2880 * dx;
        MAP(values,15,2,1,4) = static_cast<REAL>(23) / 2880 * dx;
        MAP(values,15,2,1,5) = static_cast<REAL>(-7) / 2880 * dx;
        MAP(values,15,2,1,6) = static_cast<REAL>(161) / 2880 * dx;
        MAP(values,15,2,1,7) = static_cast<REAL>(-49) / 2880 * dx;
        MAP(values,15,2,2,0) = static_cast<REAL>(-161) / 10800 * dx * dy / dz;
        MAP(values,15,2,2,1) = static_cast<REAL>(49) / 10800 * dx * dy / dz;
        MAP(values,15,2,2,2) = static_cast<REAL>(529) / 10800 * dx * dy / dz;
        MAP(values,15,2,2,3) = static_cast<REAL>(-161) / 10800 * dx * dy / dz;
        MAP(values,15,2,2,4) = static_cast<REAL>(-161) / 10800 * dx * dy / dz;
        MAP(values,15,2,2,5) = static_cast<REAL>(49) / 10800 * dx * dy / dz;
        MAP(values,15,2,2,6) = static_cast<REAL>(529) / 10800 * dx * dy / dz;
        MAP(values,15,2,2,7) = static_cast<REAL>(-161) / 10800 * dx * dy / dz;
        MAP(values,16,0,0,0) = static_cast<REAL>(-28) / 675 / dx * dy * dz;
        MAP(values,16,0,0,1) = static_cast<REAL>(-28) / 675 / dx * dy * dz;
        MAP(values,16,0,0,2) = static_cast<REAL>(92) / 675 / dx * dy * dz;
        MAP(values,16,0,0,3) = static_cast<REAL>(92) / 675 / dx * dy * dz;
        MAP(values,16,0,0,4) = static_cast<REAL>(-28) / 675 / dx * dy * dz;
        MAP(values,16,0,0,5) = static_cast<REAL>(-28) / 675 / dx * dy * dz;
        MAP(values,16,0,0,6) = static_cast<REAL>(92) / 675 / dx * dy * dz;
        MAP(values,16,0,0,7) = static_cast<REAL>(92) / 675 / dx * dy * dz;
        MAP(values,16,0,1,0) = static_cast<REAL>(-dz) / 45;
        MAP(values,16,0,1,1) = static_cast<REAL>(dz) / 45;
        MAP(values,16,0,1,2) = static_cast<REAL>(-7) / 45 * dz;
        MAP(values,16,0,1,3) = static_cast<REAL>(7) / 45 * dz;
        MAP(values,16,0,1,4) = static_cast<REAL>(-dz) / 45;
        MAP(values,16,0,1,5) = static_cast<REAL>(dz) / 45;
        MAP(values,16,0,1,6) = static_cast<REAL>(-7) / 45 * dz;
        MAP(values,16,0,1,7) = static_cast<REAL>(7) / 45 * dz;
        MAP(values,16,0,2,0) = static_cast<REAL>(-7) / 480 * dy;
        MAP(values,16,0,2,1) = static_cast<REAL>(7) / 480 * dy;
        MAP(values,16,0,2,2) = static_cast<REAL>(23) / 480 * dy;
        MAP(values,16,0,2,3) = static_cast<REAL>(-23) / 480 * dy;
        MAP(values,16,0,2,4) = static_cast<REAL>(7) / 480 * dy;
        MAP(values,16,0,2,5) = static_cast<REAL>(-7) / 480 * dy;
        MAP(values,16,0,2,6) = static_cast<REAL>(-23) / 480 * dy;
        MAP(values,16,0,2,7) = static_cast<REAL>(23) / 480 * dy;
        MAP(values,16,1,0,0) = static_cast<REAL>(dz) / 45;
        MAP(values,16,1,0,1) = static_cast<REAL>(-dz) / 45;
        MAP(values,16,1,0,2) = static_cast<REAL>(7) / 45 * dz;
        MAP(values,16,1,0,3) = static_cast<REAL>(-7) / 45 * dz;
        MAP(values,16,1,0,4) = static_cast<REAL>(dz) / 45;
        MAP(values,16,1,0,5) = static_cast<REAL>(-dz) / 45;
        MAP(values,16,1,0,6) = static_cast<REAL>(7) / 45 * dz;
        MAP(values,16,1,0,7) = static_cast<REAL>(-7) / 45 * dz;
        MAP(values,16,1,1,0) = static_cast<REAL>(-32) / 675 * dx / dy * dz;
        MAP(values,16,1,1,1) = static_cast<REAL>(-32) / 675 * dx / dy * dz;
        MAP(values,16,1,1,2) = static_cast<REAL>(-224) / 675 * dx / dy * dz;
        MAP(values,16,1,1,3) = static_cast<REAL>(-224) / 675 * dx / dy * dz;
        MAP(values,16,1,1,4) = static_cast<REAL>(-32) / 675 * dx / dy * dz;
        MAP(values,16,1,1,5) = static_cast<REAL>(-32) / 675 * dx / dy * dz;
        MAP(values,16,1,1,6) = static_cast<REAL>(-224) / 675 * dx / dy * dz;
        MAP(values,16,1,1,7) = static_cast<REAL>(-224) / 675 * dx / dy * dz;
        MAP(values,16,1,2,0) = static_cast<REAL>(dx) / 45;
        MAP(values,16,1,2,1) = static_cast<REAL>(dx) / 45;
        MAP(values,16,1,2,2) = static_cast<REAL>(7) / 45 * dx;
        MAP(values,16,1,2,3) = static_cast<REAL>(7) / 45 * dx;
        MAP(values,16,1,2,4) = static_cast<REAL>(-dx) / 45;
        MAP(values,16,1,2,5) = static_cast<REAL>(-dx) / 45;
        MAP(values,16,1,2,6) = static_cast<REAL>(-7) / 45 * dx;
        MAP(values,16,1,2,7) = static_cast<REAL>(-7) / 45 * dx;
        MAP(values,16,2,0,0) = static_cast<REAL>(-7) / 480 * dy;
        MAP(values,16,2,0,1) = static_cast<REAL>(7) / 480 * dy;
        MAP(values,16,2,0,2) = static_cast<REAL>(23) / 480 * dy;
        MAP(values,16,2,0,3) = static_cast<REAL>(-23) / 480 * dy;
        MAP(values,16,2,0,4) = static_cast<REAL>(7) / 480 * dy;
        MAP(values,16,2,0,5) = static_cast<REAL>(-7) / 480 * dy;
        MAP(values,16,2,0,6) = static_cast<REAL>(-23) / 480 * dy;
        MAP(values,16,2,0,7) = static_cast<REAL>(23) / 480 * dy;
        MAP(values,16,2,1,0) = static_cast<REAL>(-dx) / 45;
        MAP(values,16,2,1,1) = static_cast<REAL>(-dx) / 45;
        MAP(values,16,2,1,2) = static_cast<REAL>(-7) / 45 * dx;
        MAP(values,16,2,1,3) = static_cast<REAL>(-7) / 45 * dx;
        MAP(values,16,2,1,4) = static_cast<REAL>(dx) / 45;
        MAP(values,16,2,1,5) = static_cast<REAL>(dx) / 45;
        MAP(values,16,2,1,6) = static_cast<REAL>(7) / 45 * dx;
        MAP(values,16,2,1,7) = static_cast<REAL>(7) / 45 * dx;
        MAP(values,16,2,2,0) = static_cast<REAL>(-28) / 675 * dx * dy / dz;
        MAP(values,16,2,2,1) = static_cast<REAL>(-28) / 675 * dx * dy / dz;
        MAP(values,16,2,2,2) = static_cast<REAL>(92) / 675 * dx * dy / dz;
        MAP(values,16,2,2,3) = static_cast<REAL>(92) / 675 * dx * dy / dz;
        MAP(values,16,2,2,4) = static_cast<REAL>(-28) / 675 * dx * dy / dz;
        MAP(values,16,2,2,5) = static_cast<REAL>(-28) / 675 * dx * dy / dz;
        MAP(values,16,2,2,6) = static_cast<REAL>(92) / 675 * dx * dy / dz;
        MAP(values,16,2,2,7) = static_cast<REAL>(92) / 675 * dx * dy / dz;
        MAP(values,17,0,0,0) = static_cast<REAL>(7) / 1350 / dx * dy * dz;
        MAP(values,17,0,0,1) = static_cast<REAL>(49) / 1350 / dx * dy * dz;
        MAP(values,17,0,0,2) = static_cast<REAL>(-23) / 1350 / dx * dy * dz;
        MAP(values,17,0,0,3) = static_cast<REAL>(-161) / 1350 / dx * dy * dz;
        MAP(values,17,0,0,4) = static_cast<REAL>(7) / 1350 / dx * dy * dz;
        MAP(values,17,0,0,5) = static_cast<REAL>(49) / 1350 / dx * dy * dz;
        MAP(values,17,0,0,6) = static_cast<REAL>(-23) / 1350 / dx * dy * dz;
        MAP(values,17,0,0,7) = static_cast<REAL>(-161) / 1350 / dx * dy * dz;
        MAP(values,17,0,1,0) = static_cast<REAL>(-dz) / 270;
        MAP(values,17,0,1,1) = static_cast<REAL>(-7) / 270 * dz;
        MAP(values,17,0,1,2) = static_cast<REAL>(-7) / 270 * dz;
        MAP(values,17,0,1,3) = static_cast<REAL>(-49) / 270 * dz;
        MAP(values,17,0,1,4) = static_cast<REAL>(-dz) / 270;
        MAP(values,17,0,1,5) = static_cast<REAL>(-7) / 270 * dz;
        MAP(values,17,0,1,6) = static_cast<REAL>(-7) / 270 * dz;
        MAP(values,17,0,1,7) = static_cast<REAL>(-49) / 270 * dz;
        MAP(values,17,0,2,0) = static_cast<REAL>(-7) / 2880 * dy;
        MAP(values,17,0,2,1) = static_cast<REAL>(-49) / 2880 * dy;
        MAP(values,17,0,2,2) = static_cast<REAL>(23) / 2880 * dy;
        MAP(values,17,0,2,3) = static_cast<REAL>(161) / 2880 * dy;
        MAP(values,17,0,2,4) = static_cast<REAL>(7) / 2880 * dy;
        MAP(values,17,0,2,5) = static_cast<REAL>(49) / 2880 * dy;
        MAP(values,17,0,2,6) = static_cast<REAL>(-23) / 2880 * dy;
        MAP(values,17,0,2,7) = static_cast<REAL>(-161) / 2880 * dy;
        MAP(values,17,1,0,0) = static_cast<REAL>(-dz) / 270;
        MAP(values,17,1,0,1) = static_cast<REAL>(-7) / 270 * dz;
        MAP(values,17,1,0,2) = static_cast<REAL>(-7) / 270 * dz;
        MAP(values,17,1,0,3) = static_cast<REAL>(-49) / 270 * dz;
        MAP(values,17,1,0,4) = static_cast<REAL>(-dz) / 270;
        MAP(values,17,1,0,5) = static_cast<REAL>(-7) / 270 * dz;
        MAP(values,17,1,0,6) = static_cast<REAL>(-7) / 270 * dz;
        MAP(values,17,1,0,7) = static_cast<REAL>(-49) / 270 * dz;
        MAP(values,17,1,1,0) = static_cast<REAL>(7) / 1350 * dx / dy * dz;
        MAP(values,17,1,1,1) = static_cast<REAL>(-23) / 1350 * dx / dy * dz;
        MAP(values,17,1,1,2) = static_cast<REAL>(49) / 1350 * dx / dy * dz;
        MAP(values,17,1,1,3) = static_cast<REAL>(-161) / 1350 * dx / dy * dz;
        MAP(values,17,1,1,4) = static_cast<REAL>(7) / 1350 * dx / dy * dz;
        MAP(values,17,1,1,5) = static_cast<REAL>(-23) / 1350 * dx / dy * dz;
        MAP(values,17,1,1,6) = static_cast<REAL>(49) / 1350 * dx / dy * dz;
        MAP(values,17,1,1,7) = static_cast<REAL>(-161) / 1350 * dx / dy * dz;
        MAP(values,17,1,2,0) = static_cast<REAL>(-7) / 2880 * dx;
        MAP(values,17,1,2,1) = static_cast<REAL>(23) / 2880 * dx;
        MAP(values,17,1,2,2) = static_cast<REAL>(-49) / 2880 * dx;
        MAP(values,17,1,2,3) = static_cast<REAL>(161) / 2880 * dx;
        MAP(values,17,1,2,4) = static_cast<REAL>(7) / 2880 * dx;
        MAP(values,17,1,2,5) = static_cast<REAL>(-23) / 2880 * dx;
        MAP(values,17,1,2,6) = static_cast<REAL>(49) / 2880 * dx;
        MAP(values,17,1,2,7) = static_cast<REAL>(-161) / 2880 * dx;
        MAP(values,17,2,0,0) = static_cast<REAL>(7) / 2880 * dy;
        MAP(values,17,2,0,1) = static_cast<REAL>(49) / 2880 * dy;
        MAP(values,17,2,0,2) = static_cast<REAL>(-23) / 2880 * dy;
        MAP(values,17,2,0,3) = static_cast<REAL>(-161) / 2880 * dy;
        MAP(values,17,2,0,4) = static_cast<REAL>(-7) / 2880 * dy;
        MAP(values,17,2,0,5) = static_cast<REAL>(-49) / 2880 * dy;
        MAP(values,17,2,0,6) = static_cast<REAL>(23) / 2880 * dy;
        MAP(values,17,2,0,7) = static_cast<REAL>(161) / 2880 * dy;
        MAP(values,17,2,1,0) = static_cast<REAL>(7) / 2880 * dx;
        MAP(values,17,2,1,1) = static_cast<REAL>(-23) / 2880 * dx;
        MAP(values,17,2,1,2) = static_cast<REAL>(49) / 2880 * dx;
        MAP(values,17,2,1,3) = static_cast<REAL>(-161) / 2880 * dx;
        MAP(values,17,2,1,4) = static_cast<REAL>(-7) / 2880 * dx;
        MAP(values,17,2,1,5) = static_cast<REAL>(23) / 2880 * dx;
        MAP(values,17,2,1,6) = static_cast<REAL>(-49) / 2880 * dx;
        MAP(values,17,2,1,7) = static_cast<REAL>(161) / 2880 * dx;
        MAP(values,17,2,2,0) = static_cast<REAL>(49) / 10800 * dx * dy / dz;
        MAP(values,17,2,2,1) = static_cast<REAL>(-161) / 10800 * dx * dy / dz;
        MAP(values,17,2,2,2) = static_cast<REAL>(-161) / 10800 * dx * dy / dz;
        MAP(values,17,2,2,3) = static_cast<REAL>(529) / 10800 * dx * dy / dz;
        MAP(values,17,2,2,4) = static_cast<REAL>(49) / 10800 * dx * dy / dz;
        MAP(values,17,2,2,5) = static_cast<REAL>(-161) / 10800 * dx * dy / dz;
        MAP(values,17,2,2,6) = static_cast<REAL>(-161) / 10800 * dx * dy / dz;
        MAP(values,17,2,2,7) = static_cast<REAL>(529) / 10800 * dx * dy / dz;
        MAP(values,18,0,0,0) = static_cast<REAL>(1127) / 86400 / dx * dy * dz;
        MAP(values,18,0,0,1) = static_cast<REAL>(161) / 86400 / dx * dy * dz;
        MAP(values,18,0,0,2) = static_cast<REAL>(-343) / 86400 / dx * dy * dz;
        MAP(values,18,0,0,3) = static_cast<REAL>(-49) / 86400 / dx * dy * dz;
        MAP(values,18,0,0,4) = static_cast<REAL>(-3703) / 86400 / dx * dy * dz;
        MAP(values,18,0,0,5) = static_cast<REAL>(-529) / 86400 / dx * dy * dz;
        MAP(values,18,0,0,6) = static_cast<REAL>(1127) / 86400 / dx * dy * dz;
        MAP(values,18,0,0,7) = static_cast<REAL>(161) / 86400 / dx * dy * dz;
        MAP(values,18,0,1,0) = static_cast<REAL>(343) / 17280 * dz;
        MAP(values,18,0,1,1) = static_cast<REAL>(49) / 17280 * dz;
        MAP(values,18,0,1,2) = static_cast<REAL>(49) / 17280 * dz;
        MAP(values,18,0,1,3) = static_cast<REAL>(7) / 17280 * dz;
        MAP(values,18,0,1,4) = static_cast<REAL>(-1127) / 17280 * dz;
        MAP(values,18,0,1,5) = static_cast<REAL>(-161) / 17280 * dz;
        MAP(values,18,0,1,6) = static_cast<REAL>(-161) / 17280 * dz;
        MAP(values,18,0,1,7) = static_cast<REAL>(-23) / 17280 * dz;
        MAP(values,18,0,2,0) = static_cast<REAL>(161) / 17280 * dy;
        MAP(values,18,0,2,1) = static_cast<REAL>(23) / 17280 * dy;
        MAP(values,18,0,2,2) = static_cast<REAL>(-49) / 17280 * dy;
        MAP(values,18,0,2,3) = static_cast<REAL>(-7) / 17280 * dy;
        MAP(values,18,0,2,4) = static_cast<REAL>(1127) / 17280 * dy;
        MAP(values,18,0,2,5) = static_cast<REAL>(161) / 17280 * dy;
        MAP(values,18,0,2,6) = static_cast<REAL>(-343) / 17280 * dy;
        MAP(values,18,0,2,7) = static_cast<REAL>(-49) / 17280 * dy;
        MAP(values,18,1,0,0) = static_cast<REAL>(343) / 17280 * dz;
        MAP(values,18,1,0,1) = static_cast<REAL>(49) / 17280 * dz;
        MAP(values,18,1,0,2) = static_cast<REAL>(49) / 17280 * dz;
        MAP(values,18,1,0,3) = static_cast<REAL>(7) / 17280 * dz;
        MAP(values,18,1,0,4) = static_cast<REAL>(-1127) / 17280 * dz;
        MAP(values,18,1,0,5) = static_cast<REAL>(-161) / 17280 * dz;
        MAP(values,18,1,0,6) = static_cast<REAL>(-161) / 17280 * dz;
        MAP(values,18,1,0,7) = static_cast<REAL>(-23) / 17280 * dz;
        MAP(values,18,1,1,0) = static_cast<REAL>(1127) / 86400 * dx / dy * dz;
        MAP(values,18,1,1,1) = static_cast<REAL>(-343) / 86400 * dx / dy * dz;
        MAP(values,18,1,1,2) = static_cast<REAL>(161) / 86400 * dx / dy * dz;
        MAP(values,18,1,1,3) = static_cast<REAL>(-49) / 86400 * dx / dy * dz;
        MAP(values,18,1,1,4) = static_cast<REAL>(-3703) / 86400 * dx / dy * dz;
        MAP(values,18,1,1,5) = static_cast<REAL>(1127) / 86400 * dx / dy * dz;
        MAP(values,18,1,1,6) = static_cast<REAL>(-529) / 86400 * dx / dy * dz;
        MAP(values,18,1,1,7) = static_cast<REAL>(161) / 86400 * dx / dy * dz;
        MAP(values,18,1,2,0) = static_cast<REAL>(161) / 17280 * dx;
        MAP(values,18,1,2,1) = static_cast<REAL>(-49) / 17280 * dx;
        MAP(values,18,1,2,2) = static_cast<REAL>(23) / 17280 * dx;
        MAP(values,18,1,2,3) = static_cast<REAL>(-7) / 17280 * dx;
        MAP(values,18,1,2,4) = static_cast<REAL>(1127) / 17280 * dx;
        MAP(values,18,1,2,5) = static_cast<REAL>(-343) / 17280 * dx;
        MAP(values,18,1,2,6) = static_cast<REAL>(161) / 17280 * dx;
        MAP(values,18,1,2,7) = static_cast<REAL>(-49) / 17280 * dx;
        MAP(values,18,2,0,0) = static_cast<REAL>(161) / 17280 * dy;
        MAP(values,18,2,0,1) = static_cast<REAL>(23) / 17280 * dy;
        MAP(values,18,2,0,2) = static_cast<REAL>(-49) / 17280 * dy;
        MAP(values,18,2,0,3) = static_cast<REAL>(-7) / 17280 * dy;
        MAP(values,18,2,0,4) = static_cast<REAL>(1127) / 17280 * dy;
        MAP(values,18,2,0,5) = static_cast<REAL>(161) / 17280 * dy;
        MAP(values,18,2,0,6) = static_cast<REAL>(-343) / 17280 * dy;
        MAP(values,18,2,0,7) = static_cast<REAL>(-49) / 17280 * dy;
        MAP(values,18,2,1,0) = static_cast<REAL>(161) / 17280 * dx;
        MAP(values,18,2,1,1) = static_cast<REAL>(-49) / 17280 * dx;
        MAP(values,18,2,1,2) = static_cast<REAL>(23) / 17280 * dx;
        MAP(values,18,2,1,3) = static_cast<REAL>(-7) / 17280 * dx;
        MAP(values,18,2,1,4) = static_cast<REAL>(1127) / 17280 * dx;
        MAP(values,18,2,1,5) = static_cast<REAL>(-343) / 17280 * dx;
        MAP(values,18,2,1,6) = static_cast<REAL>(161) / 17280 * dx;
        MAP(values,18,2,1,7) = static_cast<REAL>(-49) / 17280 * dx;
        MAP(values,18,2,2,0) = static_cast<REAL>(-529) / 86400 * dx * dy / dz;
        MAP(values,18,2,2,1) = static_cast<REAL>(161) / 86400 * dx * dy / dz;
        MAP(values,18,2,2,2) = static_cast<REAL>(161) / 86400 * dx * dy / dz;
        MAP(values,18,2,2,3) = static_cast<REAL>(-49) / 86400 * dx * dy / dz;
        MAP(values,18,2,2,4) = static_cast<REAL>(-3703) / 86400 * dx * dy / dz;
        MAP(values,18,2,2,5) = static_cast<REAL>(1127) / 86400 * dx * dy / dz;
        MAP(values,18,2,2,6) = static_cast<REAL>(1127) / 86400 * dx * dy / dz;
        MAP(values,18,2,2,7) = static_cast<REAL>(-343) / 86400 * dx * dy / dz;
        MAP(values,19,0,0,0) = static_cast<REAL>(-161) / 10800 / dx * dy * dz;
        MAP(values,19,0,0,1) = static_cast<REAL>(-161) / 10800 / dx * dy * dz;
        MAP(values,19,0,0,2) = static_cast<REAL>(49) / 10800 / dx * dy * dz;
        MAP(values,19,0,0,3) = static_cast<REAL>(49) / 10800 / dx * dy * dz;
        MAP(values,19,0,0,4) = static_cast<REAL>(529) / 10800 / dx * dy * dz;
        MAP(values,19,0,0,5) = static_cast<REAL>(529) / 10800 / dx * dy * dz;
        MAP(values,19,0,0,6) = static_cast<REAL>(-161) / 10800 / dx * dy * dz;
        MAP(values,19,0,0,7) = static_cast<REAL>(-161) / 10800 / dx * dy * dz;
        MAP(values,19,0,1,0) = static_cast<REAL>(-49) / 2880 * dz;
        MAP(values,19,0,1,1) = static_cast<REAL>(49) / 2880 * dz;
        MAP(values,19,0,1,2) = static_cast<REAL>(-7) / 2880 * dz;
        MAP(values,19,0,1,3) = static_cast<REAL>(7) / 2880 * dz;
        MAP(values,19,0,1,4) = static_cast<REAL>(161) / 2880 * dz;
        MAP(values,19,0,1,5) = static_cast<REAL>(-161) / 2880 * dz;
        MAP(values,19,0,1,6) = static_cast<REAL>(23) / 2880 * dz;
        MAP(values,19,0,1,7) = static_cast<REAL>(-23) / 2880 * dz;
        MAP(values,19,0,2,0) = static_cast<REAL>(-23) / 2880 * dy;
        MAP(values,19,0,2,1) = static_cast<REAL>(23) / 2880 * dy;
        MAP(values,19,0,2,2) = static_cast<REAL>(7) / 2880 * dy;
        MAP(values,19,0,2,3) = static_cast<REAL>(-7) / 2880 * dy;
        MAP(values,19,0,2,4) = static_cast<REAL>(-161) / 2880 * dy;
        MAP(values,19,0,2,5) = static_cast<REAL>(161) / 2880 * dy;
        MAP(values,19,0,2,6) = static_cast<REAL>(49) / 2880 * dy;
        MAP(values,19,0,2,7) = static_cast<REAL>(-49) / 2880 * dy;
        MAP(values,19,1,0,0) = static_cast<REAL>(49) / 2880 * dz;
        MAP(values,19,1,0,1) = static_cast<REAL>(-49) / 2880 * dz;
        MAP(values,19,1,0,2) = static_cast<REAL>(7) / 2880 * dz;
        MAP(values,19,1,0,3) = static_cast<REAL>(-7) / 2880 * dz;
        MAP(values,19,1,0,4) = static_cast<REAL>(-161) / 2880 * dz;
        MAP(values,19,1,0,5) = static_cast<REAL>(161) / 2880 * dz;
        MAP(values,19,1,0,6) = static_cast<REAL>(-23) / 2880 * dz;
        MAP(values,19,1,0,7) = static_cast<REAL>(23) / 2880 * dz;
        MAP(values,19,1,1,0) = static_cast<REAL>(49) / 1350 * dx / dy * dz;
        MAP(values,19,1,1,1) = static_cast<REAL>(49) / 1350 * dx / dy * dz;
        MAP(values,19,1,1,2) = static_cast<REAL>(7) / 1350 * dx / dy * dz;
        MAP(values,19,1,1,3) = static_cast<REAL>(7) / 1350 * dx / dy * dz;
        MAP(values,19,1,1,4) = static_cast<REAL>(-161) / 1350 * dx / dy * dz;
        MAP(values,19,1,1,5) = static_cast<REAL>(-161) / 1350 * dx / dy * dz;
        MAP(values,19,1,1,6) = static_cast<REAL>(-23) / 1350 * dx / dy * dz;
        MAP(values,19,1,1,7) = static_cast<REAL>(-23) / 1350 * dx / dy * dz;
        MAP(values,19,1,2,0) = static_cast<REAL>(7) / 270 * dx;
        MAP(values,19,1,2,1) = static_cast<REAL>(7) / 270 * dx;
        MAP(values,19,1,2,2) = static_cast<REAL>(dx) / 270;
        MAP(values,19,1,2,3) = static_cast<REAL>(dx) / 270;
        MAP(values,19,1,2,4) = static_cast<REAL>(49) / 270 * dx;
        MAP(values,19,1,2,5) = static_cast<REAL>(49) / 270 * dx;
        MAP(values,19,1,2,6) = static_cast<REAL>(7) / 270 * dx;
        MAP(values,19,1,2,7) = static_cast<REAL>(7) / 270 * dx;
        MAP(values,19,2,0,0) = static_cast<REAL>(23) / 2880 * dy;
        MAP(values,19,2,0,1) = static_cast<REAL>(-23) / 2880 * dy;
        MAP(values,19,2,0,2) = static_cast<REAL>(-7) / 2880 * dy;
        MAP(values,19,2,0,3) = static_cast<REAL>(7) / 2880 * dy;
        MAP(values,19,2,0,4) = static_cast<REAL>(161) / 2880 * dy;
        MAP(values,19,2,0,5) = static_cast<REAL>(-161) / 2880 * dy;
        MAP(values,19,2,0,6) = static_cast<REAL>(-49) / 2880 * dy;
        MAP(values,19,2,0,7) = static_cast<REAL>(49) / 2880 * dy;
        MAP(values,19,2,1,0) = static_cast<REAL>(7) / 270 * dx;
        MAP(values,19,2,1,1) = static_cast<REAL>(7) / 270 * dx;
        MAP(values,19,2,1,2) = static_cast<REAL>(dx) / 270;
        MAP(values,19,2,1,3) = static_cast<REAL>(dx) / 270;
        MAP(values,19,2,1,4) = static_cast<REAL>(49) / 270 * dx;
        MAP(values,19,2,1,5) = static_cast<REAL>(49) / 270 * dx;
        MAP(values,19,2,1,6) = static_cast<REAL>(7) / 270 * dx;
        MAP(values,19,2,1,7) = static_cast<REAL>(7) / 270 * dx;
        MAP(values,19,2,2,0) = static_cast<REAL>(-23) / 1350 * dx * dy / dz;
        MAP(values,19,2,2,1) = static_cast<REAL>(-23) / 1350 * dx * dy / dz;
        MAP(values,19,2,2,2) = static_cast<REAL>(7) / 1350 * dx * dy / dz;
        MAP(values,19,2,2,3) = static_cast<REAL>(7) / 1350 * dx * dy / dz;
        MAP(values,19,2,2,4) = static_cast<REAL>(-161) / 1350 * dx * dy / dz;
        MAP(values,19,2,2,5) = static_cast<REAL>(-161) / 1350 * dx * dy / dz;
        MAP(values,19,2,2,6) = static_cast<REAL>(49) / 1350 * dx * dy / dz;
        MAP(values,19,2,2,7) = static_cast<REAL>(49) / 1350 * dx * dy / dz;
        MAP(values,20,0,0,0) = static_cast<REAL>(161) / 86400 / dx * dy * dz;
        MAP(values,20,0,0,1) = static_cast<REAL>(1127) / 86400 / dx * dy * dz;
        MAP(values,20,0,0,2) = static_cast<REAL>(-49) / 86400 / dx * dy * dz;
        MAP(values,20,0,0,3) = static_cast<REAL>(-343) / 86400 / dx * dy * dz;
        MAP(values,20,0,0,4) = static_cast<REAL>(-529) / 86400 / dx * dy * dz;
        MAP(values,20,0,0,5) = static_cast<REAL>(-3703) / 86400 / dx * dy * dz;
        MAP(values,20,0,0,6) = static_cast<REAL>(161) / 86400 / dx * dy * dz;
        MAP(values,20,0,0,7) = static_cast<REAL>(1127) / 86400 / dx * dy * dz;
        MAP(values,20,0,1,0) = static_cast<REAL>(-49) / 17280 * dz;
        MAP(values,20,0,1,1) = static_cast<REAL>(-343) / 17280 * dz;
        MAP(values,20,0,1,2) = static_cast<REAL>(-7) / 17280 * dz;
        MAP(values,20,0,1,3) = static_cast<REAL>(-49) / 17280 * dz;
        MAP(values,20,0,1,4) = static_cast<REAL>(161) / 17280 * dz;
        MAP(values,20,0,1,5) = static_cast<REAL>(1127) / 17280 * dz;
        MAP(values,20,0,1,6) = static_cast<REAL>(23) / 17280 * dz;
        MAP(values,20,0,1,7) = static_cast<REAL>(161) / 17280 * dz;
        MAP(values,20,0,2,0) = static_cast<REAL>(-23) / 17280 * dy;
        MAP(values,20,0,2,1) = static_cast<REAL>(-161) / 17280 * dy;
        MAP(values,20,0,2,2) = static_cast<REAL>(7) / 17280 * dy;
        MAP(values,20,0,2,3) = static_cast<REAL>(49) / 17280 * dy;
        MAP(values,20,0,2,4) = static_cast<REAL>(-161) / 17280 * dy;
        MAP(values,20,0,2,5) = static_cast<REAL>(-1127) / 17280 * dy;
        MAP(values,20,0,2,6) = static_cast<REAL>(49) / 17280 * dy;
        MAP(values,20,0,2,7) = static_cast<REAL>(343) / 17280 * dy;
        MAP(values,20,1,0,0) = static_cast<REAL>(-49) / 17280 * dz;
        MAP(values,20,1,0,1) = static_cast<REAL>(-343) / 17280 * dz;
        MAP(values,20,1,0,2) = static_cast<REAL>(-7) / 17280 * dz;
        MAP(values,20,1,0,3) = static_cast<REAL>(-49) / 17280 * dz;
        MAP(values,20,1,0,4) = static_cast<REAL>(161) / 17280 * dz;
        MAP(values,20,1,0,5) = static_cast<REAL>(1127) / 17280 * dz;
        MAP(values,20,1,0,6) = static_cast<REAL>(23) / 17280 * dz;
        MAP(values,20,1,0,7) = static_cast<REAL>(161) / 17280 * dz;
        MAP(values,20,1,1,0) = static_cast<REAL>(-343) / 86400 * dx / dy * dz;
        MAP(values,20,1,1,1) = static_cast<REAL>(1127) / 86400 * dx / dy * dz;
        MAP(values,20,1,1,2) = static_cast<REAL>(-49) / 86400 * dx / dy * dz;
        MAP(values,20,1,1,3) = static_cast<REAL>(161) / 86400 * dx / dy * dz;
        MAP(values,20,1,1,4) = static_cast<REAL>(1127) / 86400 * dx / dy * dz;
        MAP(values,20,1,1,5) = static_cast<REAL>(-3703) / 86400 * dx / dy * dz;
        MAP(values,20,1,1,6) = static_cast<REAL>(161) / 86400 * dx / dy * dz;
        MAP(values,20,1,1,7) = static_cast<REAL>(-529) / 86400 * dx / dy * dz;
        MAP(values,20,1,2,0) = static_cast<REAL>(-49) / 17280 * dx;
        MAP(values,20,1,2,1) = static_cast<REAL>(161) / 17280 * dx;
        MAP(values,20,1,2,2) = static_cast<REAL>(-7) / 17280 * dx;
        MAP(values,20,1,2,3) = static_cast<REAL>(23) / 17280 * dx;
        MAP(values,20,1,2,4) = static_cast<REAL>(-343) / 17280 * dx;
        MAP(values,20,1,2,5) = static_cast<REAL>(1127) / 17280 * dx;
        MAP(values,20,1,2,6) = static_cast<REAL>(-49) / 17280 * dx;
        MAP(values,20,1,2,7) = static_cast<REAL>(161) / 17280 * dx;
        MAP(values,20,2,0,0) = static_cast<REAL>(-23) / 17280 * dy;
        MAP(values,20,2,0,1) = static_cast<REAL>(-161) / 17280 * dy;
        MAP(values,20,2,0,2) = static_cast<REAL>(7) / 17280 * dy;
        MAP(values,20,2,0,3) = static_cast<REAL>(49) / 17280 * dy;
        MAP(values,20,2,0,4) = static_cast<REAL>(-161) / 17280 * dy;
        MAP(values,20,2,0,5) = static_cast<REAL>(-1127) / 17280 * dy;
        MAP(values,20,2,0,6) = static_cast<REAL>(49) / 17280 * dy;
        MAP(values,20,2,0,7) = static_cast<REAL>(343) / 17280 * dy;
        MAP(values,20,2,1,0) = static_cast<REAL>(-49) / 17280 * dx;
        MAP(values,20,2,1,1) = static_cast<REAL>(161) / 17280 * dx;
        MAP(values,20,2,1,2) = static_cast<REAL>(-7) / 17280 * dx;
        MAP(values,20,2,1,3) = static_cast<REAL>(23) / 17280 * dx;
        MAP(values,20,2,1,4) = static_cast<REAL>(-343) / 17280 * dx;
        MAP(values,20,2,1,5) = static_cast<REAL>(1127) / 17280 * dx;
        MAP(values,20,2,1,6) = static_cast<REAL>(-49) / 17280 * dx;
        MAP(values,20,2,1,7) = static_cast<REAL>(161) / 17280 * dx;
        MAP(values,20,2,2,0) = static_cast<REAL>(161) / 86400 * dx * dy / dz;
        MAP(values,20,2,2,1) = static_cast<REAL>(-529) / 86400 * dx * dy / dz;
        MAP(values,20,2,2,2) = static_cast<REAL>(-49) / 86400 * dx * dy / dz;
        MAP(values,20,2,2,3) = static_cast<REAL>(161) / 86400 * dx * dy / dz;
        MAP(values,20,2,2,4) = static_cast<REAL>(1127) / 86400 * dx * dy / dz;
        MAP(values,20,2,2,5) = static_cast<REAL>(-3703) / 86400 * dx * dy / dz;
        MAP(values,20,2,2,6) = static_cast<REAL>(-343) / 86400 * dx * dy / dz;
        MAP(values,20,2,2,7) = static_cast<REAL>(1127) / 86400 * dx * dy / dz;
        MAP(values,21,0,0,0) = static_cast<REAL>(49) / 1350 / dx * dy * dz;
        MAP(values,21,0,0,1) = static_cast<REAL>(7) / 1350 / dx * dy * dz;
        MAP(values,21,0,0,2) = static_cast<REAL>(49) / 1350 / dx * dy * dz;
        MAP(values,21,0,0,3) = static_cast<REAL>(7) / 1350 / dx * dy * dz;
        MAP(values,21,0,0,4) = static_cast<REAL>(-161) / 1350 / dx * dy * dz;
        MAP(values,21,0,0,5) = static_cast<REAL>(-23) / 1350 / dx * dy * dz;
        MAP(values,21,0,0,6) = static_cast<REAL>(-161) / 1350 / dx * dy * dz;
        MAP(values,21,0,0,7) = static_cast<REAL>(-23) / 1350 / dx * dy * dz;
        MAP(values,21,0,1,0) = static_cast<REAL>(49) / 2880 * dz;
        MAP(values,21,0,1,1) = static_cast<REAL>(7) / 2880 * dz;
        MAP(values,21,0,1,2) = static_cast<REAL>(-49) / 2880 * dz;
        MAP(values,21,0,1,3) = static_cast<REAL>(-7) / 2880 * dz;
        MAP(values,21,0,1,4) = static_cast<REAL>(-161) / 2880 * dz;
        MAP(values,21,0,1,5) = static_cast<REAL>(-23) / 2880 * dz;
        MAP(values,21,0,1,6) = static_cast<REAL>(161) / 2880 * dz;
        MAP(values,21,0,1,7) = static_cast<REAL>(23) / 2880 * dz;
        MAP(values,21,0,2,0) = static_cast<REAL>(7) / 270 * dy;
        MAP(values,21,0,2,1) = static_cast<REAL>(dy) / 270;
        MAP(values,21,0,2,2) = static_cast<REAL>(7) / 270 * dy;
        MAP(values,21,0,2,3) = static_cast<REAL>(dy) / 270;
        MAP(values,21,0,2,4) = static_cast<REAL>(49) / 270 * dy;
        MAP(values,21,0,2,5) = static_cast<REAL>(7) / 270 * dy;
        MAP(values,21,0,2,6) = static_cast<REAL>(49) / 270 * dy;
        MAP(values,21,0,2,7) = static_cast<REAL>(7) / 270 * dy;
        MAP(values,21,1,0,0) = static_cast<REAL>(-49) / 2880 * dz;
        MAP(values,21,1,0,1) = static_cast<REAL>(-7) / 2880 * dz;
        MAP(values,21,1,0,2) = static_cast<REAL>(49) / 2880 * dz;
        MAP(values,21,1,0,3) = static_cast<REAL>(7) / 2880 * dz;
        MAP(values,21,1,0,4) = static_cast<REAL>(161) / 2880 * dz;
        MAP(values,21,1,0,5) = static_cast<REAL>(23) / 2880 * dz;
        MAP(values,21,1,0,6) = static_cast<REAL>(-161) / 2880 * dz;
        MAP(values,21,1,0,7) = static_cast<REAL>(-23) / 2880 * dz;
        MAP(values,21,1,1,0) = static_cast<REAL>(-161) / 10800 * dx / dy * dz;
        MAP(values,21,1,1,1) = static_cast<REAL>(49) / 10800 * dx / dy * dz;
        MAP(values,21,1,1,2) = static_cast<REAL>(-161) / 10800 * dx / dy * dz;
        MAP(values,21,1,1,3) = static_cast<REAL>(49) / 10800 * dx / dy * dz;
        MAP(values,21,1,1,4) = static_cast<REAL>(529) / 10800 * dx / dy * dz;
        MAP(values,21,1,1,5) = static_cast<REAL>(-161) / 10800 * dx / dy * dz;
        MAP(values,21,1,1,6) = static_cast<REAL>(529) / 10800 * dx / dy * dz;
        MAP(values,21,1,1,7) = static_cast<REAL>(-161) / 10800 * dx / dy * dz;
        MAP(values,21,1,2,0) = static_cast<REAL>(-23) / 2880 * dx;
        MAP(values,21,1,2,1) = static_cast<REAL>(7) / 2880 * dx;
        MAP(values,21,1,2,2) = static_cast<REAL>(23) / 2880 * dx;
        MAP(values,21,1,2,3) = static_cast<REAL>(-7) / 2880 * dx;
        MAP(values,21,1,2,4) = static_cast<REAL>(-161) / 2880 * dx;
        MAP(values,21,1,2,5) = static_cast<REAL>(49) / 2880 * dx;
        MAP(values,21,1,2,6) = static_cast<REAL>(161) / 2880 * dx;
        MAP(values,21,1,2,7) = static_cast<REAL>(-49) / 2880 * dx;
        MAP(values,21,2,0,0) = static_cast<REAL>(7) / 270 * dy;
        MAP(values,21,2,0,1) = static_cast<REAL>(dy) / 270;
        MAP(values,21,2,0,2) = static_cast<REAL>(7) / 270 * dy;
        MAP(values,21,2,0,3) = static_cast<REAL>(dy) / 270;
        MAP(values,21,2,0,4) = static_cast<REAL>(49) / 270 * dy;
        MAP(values,21,2,0,5) = static_cast<REAL>(7) / 270 * dy;
        MAP(values,21,2,0,6) = static_cast<REAL>(49) / 270 * dy;
        MAP(values,21,2,0,7) = static_cast<REAL>(7) / 270 * dy;
        MAP(values,21,2,1,0) = static_cast<REAL>(23) / 2880 * dx;
        MAP(values,21,2,1,1) = static_cast<REAL>(-7) / 2880 * dx;
        MAP(values,21,2,1,2) = static_cast<REAL>(-23) / 2880 * dx;
        MAP(values,21,2,1,3) = static_cast<REAL>(7) / 2880 * dx;
        MAP(values,21,2,1,4) = static_cast<REAL>(161) / 2880 * dx;
        MAP(values,21,2,1,5) = static_cast<REAL>(-49) / 2880 * dx;
        MAP(values,21,2,1,6) = static_cast<REAL>(-161) / 2880 * dx;
        MAP(values,21,2,1,7) = static_cast<REAL>(49) / 2880 * dx;
        MAP(values,21,2,2,0) = static_cast<REAL>(-23) / 1350 * dx * dy / dz;
        MAP(values,21,2,2,1) = static_cast<REAL>(7) / 1350 * dx * dy / dz;
        MAP(values,21,2,2,2) = static_cast<REAL>(-23) / 1350 * dx * dy / dz;
        MAP(values,21,2,2,3) = static_cast<REAL>(7) / 1350 * dx * dy / dz;
        MAP(values,21,2,2,4) = static_cast<REAL>(-161) / 1350 * dx * dy / dz;
        MAP(values,21,2,2,5) = static_cast<REAL>(49) / 1350 * dx * dy / dz;
        MAP(values,21,2,2,6) = static_cast<REAL>(-161) / 1350 * dx * dy / dz;
        MAP(values,21,2,2,7) = static_cast<REAL>(49) / 1350 * dx * dy / dz;
        MAP(values,22,0,0,0) = static_cast<REAL>(-28) / 675 / dx * dy * dz;
        MAP(values,22,0,0,1) = static_cast<REAL>(-28) / 675 / dx * dy * dz;
        MAP(values,22,0,0,2) = static_cast<REAL>(-28) / 675 / dx * dy * dz;
        MAP(values,22,0,0,3) = static_cast<REAL>(-28) / 675 / dx * dy * dz;
        MAP(values,22,0,0,4) = static_cast<REAL>(92) / 675 / dx * dy * dz;
        MAP(values,22,0,0,5) = static_cast<REAL>(92) / 675 / dx * dy * dz;
        MAP(values,22,0,0,6) = static_cast<REAL>(92) / 675 / dx * dy * dz;
        MAP(values,22,0,0,7) = static_cast<REAL>(92) / 675 / dx * dy * dz;
        MAP(values,22,0,1,0) = static_cast<REAL>(-7) / 480 * dz;
        MAP(values,22,0,1,1) = static_cast<REAL>(7) / 480 * dz;
        MAP(values,22,0,1,2) = static_cast<REAL>(7) / 480 * dz;
        MAP(values,22,0,1,3) = static_cast<REAL>(-7) / 480 * dz;
        MAP(values,22,0,1,4) = static_cast<REAL>(23) / 480 * dz;
        MAP(values,22,0,1,5) = static_cast<REAL>(-23) / 480 * dz;
        MAP(values,22,0,1,6) = static_cast<REAL>(-23) / 480 * dz;
        MAP(values,22,0,1,7) = static_cast<REAL>(23) / 480 * dz;
        MAP(values,22,0,2,0) = static_cast<REAL>(-dy) / 45;
        MAP(values,22,0,2,1) = static_cast<REAL>(dy) / 45;
        MAP(values,22,0,2,2) = static_cast<REAL>(-dy) / 45;
        MAP(values,22,0,2,3) = static_cast<REAL>(dy) / 45;
        MAP(values,22,0,2,4) = static_cast<REAL>(-7) / 45 * dy;
        MAP(values,22,0,2,5) = static_cast<REAL>(7) / 45 * dy;
        MAP(values,22,0,2,6) = static_cast<REAL>(-7) / 45 * dy;
        MAP(values,22,0,2,7) = static_cast<REAL>(7) / 45 * dy;
        MAP(values,22,1,0,0) = static_cast<REAL>(-7) / 480 * dz;
        MAP(values,22,1,0,1) = static_cast<REAL>(7) / 480 * dz;
        MAP(values,22,1,0,2) = static_cast<REAL>(7) / 480 * dz;
        MAP(values,22,1,0,3) = static_cast<REAL>(-7) / 480 * dz;
        MAP(values,22,1,0,4) = static_cast<REAL>(23) / 480 * dz;
        MAP(values,22,1,0,5) = static_cast<REAL>(-23) / 480 * dz;
        MAP(values,22,1,0,6) = static_cast<REAL>(-23) / 480 * dz;
        MAP(values,22,1,0,7) = static_cast<REAL>(23) / 480 * dz;
        MAP(values,22,1,1,0) = static_cast<REAL>(-28) / 675 * dx / dy * dz;
        MAP(values,22,1,1,1) = static_cast<REAL>(-28) / 675 * dx / dy * dz;
        MAP(values,22,1,1,2) = static_cast<REAL>(-28) / 675 * dx / dy * dz;
        MAP(values,22,1,1,3) = static_cast<REAL>(-28) / 675 * dx / dy * dz;
        MAP(values,22,1,1,4) = static_cast<REAL>(92) / 675 * dx / dy * dz;
        MAP(values,22,1,1,5) = static_cast<REAL>(92) / 675 * dx / dy * dz;
        MAP(values,22,1,1,6) = static_cast<REAL>(92) / 675 * dx / dy * dz;
        MAP(values,22,1,1,7) = static_cast<REAL>(92) / 675 * dx / dy * dz;
        MAP(values,22,1,2,0) = static_cast<REAL>(-dx) / 45;
        MAP(values,22,1,2,1) = static_cast<REAL>(-dx) / 45;
        MAP(values,22,1,2,2) = static_cast<REAL>(dx) / 45;
        MAP(values,22,1,2,3) = static_cast<REAL>(dx) / 45;
        MAP(values,22,1,2,4) = static_cast<REAL>(-7) / 45 * dx;
        MAP(values,22,1,2,5) = static_cast<REAL>(-7) / 45 * dx;
        MAP(values,22,1,2,6) = static_cast<REAL>(7) / 45 * dx;
        MAP(values,22,1,2,7) = static_cast<REAL>(7) / 45 * dx;
        MAP(values,22,2,0,0) = static_cast<REAL>(dy) / 45;
        MAP(values,22,2,0,1) = static_cast<REAL>(-dy) / 45;
        MAP(values,22,2,0,2) = static_cast<REAL>(dy) / 45;
        MAP(values,22,2,0,3) = static_cast<REAL>(-dy) / 45;
        MAP(values,22,2,0,4) = static_cast<REAL>(7) / 45 * dy;
        MAP(values,22,2,0,5) = static_cast<REAL>(-7) / 45 * dy;
        MAP(values,22,2,0,6) = static_cast<REAL>(7) / 45 * dy;
        MAP(values,22,2,0,7) = static_cast<REAL>(-7) / 45 * dy;
        MAP(values,22,2,1,0) = static_cast<REAL>(dx) / 45;
        MAP(values,22,2,1,1) = static_cast<REAL>(dx) / 45;
        MAP(values,22,2,1,2) = static_cast<REAL>(-dx) / 45;
        MAP(values,22,2,1,3) = static_cast<REAL>(-dx) / 45;
        MAP(values,22,2,1,4) = static_cast<REAL>(7) / 45 * dx;
        MAP(values,22,2,1,5) = static_cast<REAL>(7) / 45 * dx;
        MAP(values,22,2,1,6) = static_cast<REAL>(-7) / 45 * dx;
        MAP(values,22,2,1,7) = static_cast<REAL>(-7) / 45 * dx;
        MAP(values,22,2,2,0) = static_cast<REAL>(-32) / 675 * dx * dy / dz;
        MAP(values,22,2,2,1) = static_cast<REAL>(-32) / 675 * dx * dy / dz;
        MAP(values,22,2,2,2) = static_cast<REAL>(-32) / 675 * dx * dy / dz;
        MAP(values,22,2,2,3) = static_cast<REAL>(-32) / 675 * dx * dy / dz;
        MAP(values,22,2,2,4) = static_cast<REAL>(-224) / 675 * dx * dy / dz;
        MAP(values,22,2,2,5) = static_cast<REAL>(-224) / 675 * dx * dy / dz;
        MAP(values,22,2,2,6) = static_cast<REAL>(-224) / 675 * dx * dy / dz;
        MAP(values,22,2,2,7) = static_cast<REAL>(-224) / 675 * dx * dy / dz;
        MAP(values,23,0,0,0) = static_cast<REAL>(7) / 1350 / dx * dy * dz;
        MAP(values,23,0,0,1) = static_cast<REAL>(49) / 1350 / dx * dy * dz;
        MAP(values,23,0,0,2) = static_cast<REAL>(7) / 1350 / dx * dy * dz;
        MAP(values,23,0,0,3) = static_cast<REAL>(49) / 1350 / dx * dy * dz;
        MAP(values,23,0,0,4) = static_cast<REAL>(-23) / 1350 / dx * dy * dz;
        MAP(values,23,0,0,5) = static_cast<REAL>(-161) / 1350 / dx * dy * dz;
        MAP(values,23,0,0,6) = static_cast<REAL>(-23) / 1350 / dx * dy * dz;
        MAP(values,23,0,0,7) = static_cast<REAL>(-161) / 1350 / dx * dy * dz;
        MAP(values,23,0,1,0) = static_cast<REAL>(-7) / 2880 * dz;
        MAP(values,23,0,1,1) = static_cast<REAL>(-49) / 2880 * dz;
        MAP(values,23,0,1,2) = static_cast<REAL>(7) / 2880 * dz;
        MAP(values,23,0,1,3) = static_cast<REAL>(49) / 2880 * dz;
        MAP(values,23,0,1,4) = static_cast<REAL>(23) / 2880 * dz;
        MAP(values,23,0,1,5) = static_cast<REAL>(161) / 2880 * dz;
        MAP(values,23,0,1,6) = static_cast<REAL>(-23) / 2880 * dz;
        MAP(values,23,0,1,7) = static_cast<REAL>(-161) / 2880 * dz;
        MAP(values,23,0,2,0) = static_cast<REAL>(-dy) / 270;
        MAP(values,23,0,2,1) = static_cast<REAL>(-7) / 270 * dy;
        MAP(values,23,0,2,2) = static_cast<REAL>(-dy) / 270;
        MAP(values,23,0,2,3) = static_cast<REAL>(-7) / 270 * dy;
        MAP(values,23,0,2,4) = static_cast<REAL>(-7) / 270 * dy;
        MAP(values,23,0,2,5) = static_cast<REAL>(-49) / 270 * dy;
        MAP(values,23,0,2,6) = static_cast<REAL>(-7) / 270 * dy;
        MAP(values,23,0,2,7) = static_cast<REAL>(-49) / 270 * dy;
        MAP(values,23,1,0,0) = static_cast<REAL>(7) / 2880 * dz;
        MAP(values,23,1,0,1) = static_cast<REAL>(49) / 2880 * dz;
        MAP(values,23,1,0,2) = static_cast<REAL>(-7) / 2880 * dz;
        MAP(values,23,1,0,3) = static_cast<REAL>(-49) / 2880 * dz;
        MAP(values,23,1,0,4) = static_cast<REAL>(-23) / 2880 * dz;
        MAP(values,23,1,0,5) = static_cast<REAL>(-161) / 2880 * dz;
        MAP(values,23,1,0,6) = static_cast<REAL>(23) / 2880 * dz;
        MAP(values,23,1,0,7) = static_cast<REAL>(161) / 2880 * dz;
        MAP(values,23,1,1,0) = static_cast<REAL>(49) / 10800 * dx / dy * dz;
        MAP(values,23,1,1,1) = static_cast<REAL>(-161) / 10800 * dx / dy * dz;
        MAP(values,23,1,1,2) = static_cast<REAL>(49) / 10800 * dx / dy * dz;
        MAP(values,23,1,1,3) = static_cast<REAL>(-161) / 10800 * dx / dy * dz;
        MAP(values,23,1,1,4) = static_cast<REAL>(-161) / 10800 * dx / dy * dz;
        MAP(values,23,1,1,5) = static_cast<REAL>(529) / 10800 * dx / dy * dz;
        MAP(values,23,1,1,6) = static_cast<REAL>(-161) / 10800 * dx / dy * dz;
        MAP(values,23,1,1,7) = static_cast<REAL>(529) / 10800 * dx / dy * dz;
        MAP(values,23,1,2,0) = static_cast<REAL>(7) / 2880 * dx;
        MAP(values,23,1,2,1) = static_cast<REAL>(-23) / 2880 * dx;
        MAP(values,23,1,2,2) = static_cast<REAL>(-7) / 2880 * dx;
        MAP(values,23,1,2,3) = static_cast<REAL>(23) / 2880 * dx;
        MAP(values,23,1,2,4) = static_cast<REAL>(49) / 2880 * dx;
        MAP(values,23,1,2,5) = static_cast<REAL>(-161) / 2880 * dx;
        MAP(values,23,1,2,6) = static_cast<REAL>(-49) / 2880 * dx;
        MAP(values,23,1,2,7) = static_cast<REAL>(161) / 2880 * dx;
        MAP(values,23,2,0,0) = static_cast<REAL>(-dy) / 270;
        MAP(values,23,2,0,1) = static_cast<REAL>(-7) / 270 * dy;
        MAP(values,23,2,0,2) = static_cast<REAL>(-dy) / 270;
        MAP(values,23,2,0,3) = static_cast<REAL>(-7) / 270 * dy;
        MAP(values,23,2,0,4) = static_cast<REAL>(-7) / 270 * dy;
        MAP(values,23,2,0,5) = static_cast<REAL>(-49) / 270 * dy;
        MAP(values,23,2,0,6) = static_cast<REAL>(-7) / 270 * dy;
        MAP(values,23,2,0,7) = static_cast<REAL>(-49) / 270 * dy;
        MAP(values,23,2,1,0) = static_cast<REAL>(-7) / 2880 * dx;
        MAP(values,23,2,1,1) = static_cast<REAL>(23) / 2880 * dx;
        MAP(values,23,2,1,2) = static_cast<REAL>(7) / 2880 * dx;
        MAP(values,23,2,1,3) = static_cast<REAL>(-23) / 2880 * dx;
        MAP(values,23,2,1,4) = static_cast<REAL>(-49) / 2880 * dx;
        MAP(values,23,2,1,5) = static_cast<REAL>(161) / 2880 * dx;
        MAP(values,23,2,1,6) = static_cast<REAL>(49) / 2880 * dx;
        MAP(values,23,2,1,7) = static_cast<REAL>(-161) / 2880 * dx;
        MAP(values,23,2,2,0) = static_cast<REAL>(7) / 1350 * dx * dy / dz;
        MAP(values,23,2,2,1) = static_cast<REAL>(-23) / 1350 * dx * dy / dz;
        MAP(values,23,2,2,2) = static_cast<REAL>(7) / 1350 * dx * dy / dz;
        MAP(values,23,2,2,3) = static_cast<REAL>(-23) / 1350 * dx * dy / dz;
        MAP(values,23,2,2,4) = static_cast<REAL>(49) / 1350 * dx * dy / dz;
        MAP(values,23,2,2,5) = static_cast<REAL>(-161) / 1350 * dx * dy / dz;
        MAP(values,23,2,2,6) = static_cast<REAL>(49) / 1350 * dx * dy / dz;
        MAP(values,23,2,2,7) = static_cast<REAL>(-161) / 1350 * dx * dy / dz;
        MAP(values,24,0,0,0) = static_cast<REAL>(-343) / 86400 / dx * dy * dz;
        MAP(values,24,0,0,1) = static_cast<REAL>(-49) / 86400 / dx * dy * dz;
        MAP(values,24,0,0,2) = static_cast<REAL>(1127) / 86400 / dx * dy * dz;
        MAP(values,24,0,0,3) = static_cast<REAL>(161) / 86400 / dx * dy * dz;
        MAP(values,24,0,0,4) = static_cast<REAL>(1127) / 86400 / dx * dy * dz;
        MAP(values,24,0,0,5) = static_cast<REAL>(161) / 86400 / dx * dy * dz;
        MAP(values,24,0,0,6) = static_cast<REAL>(-3703) / 86400 / dx * dy * dz;
        MAP(values,24,0,0,7) = static_cast<REAL>(-529) / 86400 / dx * dy * dz;
        MAP(values,24,0,1,0) = static_cast<REAL>(-49) / 17280 * dz;
        MAP(values,24,0,1,1) = static_cast<REAL>(-7) / 17280 * dz;
        MAP(values,24,0,1,2) = static_cast<REAL>(-343) / 17280 * dz;
        MAP(values,24,0,1,3) = static_cast<REAL>(-49) / 17280 * dz;
        MAP(values,24,0,1,4) = static_cast<REAL>(161) / 17280 * dz;
        MAP(values,24,0,1,5) = static_cast<REAL>(23) / 17280 * dz;
        MAP(values,24,0,1,6) = static_cast<REAL>(1127) / 17280 * dz;
        MAP(values,24,0,1,7) = static_cast<REAL>(161) / 17280 * dz;
        MAP(values,24,0,2,0) = static_cast<REAL>(-49) / 17280 * dy;
        MAP(values,24,0,2,1) = static_cast<REAL>(-7) / 17280 * dy;
        MAP(values,24,0,2,2) = static_cast<REAL>(161) / 17280 * dy;
        MAP(values,24,0,2,3) = static_cast<REAL>(23) / 17280 * dy;
        MAP(values,24,0,2,4) = static_cast<REAL>(-343) / 17280 * dy;
        MAP(values,24,0,2,5) = static_cast<REAL>(-49) / 17280 * dy;
        MAP(values,24,0,2,6) = static_cast<REAL>(1127) / 17280 * dy;
        MAP(values,24,0,2,7) = static_cast<REAL>(161) / 17280 * dy;
        MAP(values,24,1,0,0) = static_cast<REAL>(-49) / 17280 * dz;
        MAP(values,24,1,0,1) = static_cast<REAL>(-7) / 17280 * dz;
        MAP(values,24,1,0,2) = static_cast<REAL>(-343) / 17280 * dz;
        MAP(values,24,1,0,3) = static_cast<REAL>(-49) / 17280 * dz;
        MAP(values,24,1,0,4) = static_cast<REAL>(161) / 17280 * dz;
        MAP(values,24,1,0,5) = static_cast<REAL>(23) / 17280 * dz;
        MAP(values,24,1,0,6) = static_cast<REAL>(1127) / 17280 * dz;
        MAP(values,24,1,0,7) = static_cast<REAL>(161) / 17280 * dz;
        MAP(values,24,1,1,0) = static_cast<REAL>(161) / 86400 * dx / dy * dz;
        MAP(values,24,1,1,1) = static_cast<REAL>(-49) / 86400 * dx / dy * dz;
        MAP(values,24,1,1,2) = static_cast<REAL>(1127) / 86400 * dx / dy * dz;
        MAP(values,24,1,1,3) = static_cast<REAL>(-343) / 86400 * dx / dy * dz;
        MAP(values,24,1,1,4) = static_cast<REAL>(-529) / 86400 * dx / dy * dz;
        MAP(values,24,1,1,5) = static_cast<REAL>(161) / 86400 * dx / dy * dz;
        MAP(values,24,1,1,6) = static_cast<REAL>(-3703) / 86400 * dx / dy * dz;
        MAP(values,24,1,1,7) = static_cast<REAL>(1127) / 86400 * dx / dy * dz;
        MAP(values,24,1,2,0) = static_cast<REAL>(-23) / 17280 * dx;
        MAP(values,24,1,2,1) = static_cast<REAL>(7) / 17280 * dx;
        MAP(values,24,1,2,2) = static_cast<REAL>(-161) / 17280 * dx;
        MAP(values,24,1,2,3) = static_cast<REAL>(49) / 17280 * dx;
        MAP(values,24,1,2,4) = static_cast<REAL>(-161) / 17280 * dx;
        MAP(values,24,1,2,5) = static_cast<REAL>(49) / 17280 * dx;
        MAP(values,24,1,2,6) = static_cast<REAL>(-1127) / 17280 * dx;
        MAP(values,24,1,2,7) = static_cast<REAL>(343) / 17280 * dx;
        MAP(values,24,2,0,0) = static_cast<REAL>(-49) / 17280 * dy;
        MAP(values,24,2,0,1) = static_cast<REAL>(-7) / 17280 * dy;
        MAP(values,24,2,0,2) = static_cast<REAL>(161) / 17280 * dy;
        MAP(values,24,2,0,3) = static_cast<REAL>(23) / 17280 * dy;
        MAP(values,24,2,0,4) = static_cast<REAL>(-343) / 17280 * dy;
        MAP(values,24,2,0,5) = static_cast<REAL>(-49) / 17280 * dy;
        MAP(values,24,2,0,6) = static_cast<REAL>(1127) / 17280 * dy;
        MAP(values,24,2,0,7) = static_cast<REAL>(161) / 17280 * dy;
        MAP(values,24,2,1,0) = static_cast<REAL>(-23) / 17280 * dx;
        MAP(values,24,2,1,1) = static_cast<REAL>(7) / 17280 * dx;
        MAP(values,24,2,1,2) = static_cast<REAL>(-161) / 17280 * dx;
        MAP(values,24,2,1,3) = static_cast<REAL>(49) / 17280 * dx;
        MAP(values,24,2,1,4) = static_cast<REAL>(-161) / 17280 * dx;
        MAP(values,24,2,1,5) = static_cast<REAL>(49) / 17280 * dx;
        MAP(values,24,2,1,6) = static_cast<REAL>(-1127) / 17280 * dx;
        MAP(values,24,2,1,7) = static_cast<REAL>(343) / 17280 * dx;
        MAP(values,24,2,2,0) = static_cast<REAL>(161) / 86400 * dx * dy / dz;
        MAP(values,24,2,2,1) = static_cast<REAL>(-49) / 86400 * dx * dy / dz;
        MAP(values,24,2,2,2) = static_cast<REAL>(-529) / 86400 * dx * dy / dz;
        MAP(values,24,2,2,3) = static_cast<REAL>(161) / 86400 * dx * dy / dz;
        MAP(values,24,2,2,4) = static_cast<REAL>(1127) / 86400 * dx * dy / dz;
        MAP(values,24,2,2,5) = static_cast<REAL>(-343) / 86400 * dx * dy / dz;
        MAP(values,24,2,2,6) = static_cast<REAL>(-3703) / 86400 * dx * dy / dz;
        MAP(values,24,2,2,7) = static_cast<REAL>(1127) / 86400 * dx * dy / dz;
        MAP(values,25,0,0,0) = static_cast<REAL>(49) / 10800 / dx * dy * dz;
        MAP(values,25,0,0,1) = static_cast<REAL>(49) / 10800 / dx * dy * dz;
        MAP(values,25,0,0,2) = static_cast<REAL>(-161) / 10800 / dx * dy * dz;
        MAP(values,25,0,0,3) = static_cast<REAL>(-161) / 10800 / dx * dy * dz;
        MAP(values,25,0,0,4) = static_cast<REAL>(-161) / 10800 / dx * dy * dz;
        MAP(values,25,0,0,5) = static_cast<REAL>(-161) / 10800 / dx * dy * dz;
        MAP(values,25,0,0,6) = static_cast<REAL>(529) / 10800 / dx * dy * dz;
        MAP(values,25,0,0,7) = static_cast<REAL>(529) / 10800 / dx * dy * dz;
        MAP(values,25,0,1,0) = static_cast<REAL>(7) / 2880 * dz;
        MAP(values,25,0,1,1) = static_cast<REAL>(-7) / 2880 * dz;
        MAP(values,25,0,1,2) = static_cast<REAL>(49) / 2880 * dz;
        MAP(values,25,0,1,3) = static_cast<REAL>(-49) / 2880 * dz;
        MAP(values,25,0,1,4) = static_cast<REAL>(-23) / 2880 * dz;
        MAP(values,25,0,1,5) = static_cast<REAL>(23) / 2880 * dz;
        MAP(values,25,0,1,6) = static_cast<REAL>(-161) / 2880 * dz;
        MAP(values,25,0,1,7) = static_cast<REAL>(161) / 2880 * dz;
        MAP(values,25,0,2,0) = static_cast<REAL>(7) / 2880 * dy;
        MAP(values,25,0,2,1) = static_cast<REAL>(-7) / 2880 * dy;
        MAP(values,25,0,2,2) = static_cast<REAL>(-23) / 2880 * dy;
        MAP(values,25,0,2,3) = static_cast<REAL>(23) / 2880 * dy;
        MAP(values,25,0,2,4) = static_cast<REAL>(49) / 2880 * dy;
        MAP(values,25,0,2,5) = static_cast<REAL>(-49) / 2880 * dy;
        MAP(values,25,0,2,6) = static_cast<REAL>(-161) / 2880 * dy;
        MAP(values,25,0,2,7) = static_cast<REAL>(161) / 2880 * dy;
        MAP(values,25,1,0,0) = static_cast<REAL>(-7) / 2880 * dz;
        MAP(values,25,1,0,1) = static_cast<REAL>(7) / 2880 * dz;
        MAP(values,25,1,0,2) = static_cast<REAL>(-49) / 2880 * dz;
        MAP(values,25,1,0,3) = static_cast<REAL>(49) / 2880 * dz;
        MAP(values,25,1,0,4) = static_cast<REAL>(23) / 2880 * dz;
        MAP(values,25,1,0,5) = static_cast<REAL>(-23) / 2880 * dz;
        MAP(values,25,1,0,6) = static_cast<REAL>(161) / 2880 * dz;
        MAP(values,25,1,0,7) = static_cast<REAL>(-161) / 2880 * dz;
        MAP(values,25,1,1,0) = static_cast<REAL>(7) / 1350 * dx / dy * dz;
        MAP(values,25,1,1,1) = static_cast<REAL>(7) / 1350 * dx / dy * dz;
        MAP(values,25,1,1,2) = static_cast<REAL>(49) / 1350 * dx / dy * dz;
        MAP(values,25,1,1,3) = static_cast<REAL>(49) / 1350 * dx / dy * dz;
        MAP(values,25,1,1,4) = static_cast<REAL>(-23) / 1350 * dx / dy * dz;
        MAP(values,25,1,1,5) = static_cast<REAL>(-23) / 1350 * dx / dy * dz;
        MAP(values,25,1,1,6) = static_cast<REAL>(-161) / 1350 * dx / dy * dz;
        MAP(values,25,1,1,7) = static_cast<REAL>(-161) / 1350 * dx / dy * dz;
        MAP(values,25,1,2,0) = static_cast<REAL>(-dx) / 270;
        MAP(values,25,1,2,1) = static_cast<REAL>(-dx) / 270;
        MAP(values,25,1,2,2) = static_cast<REAL>(-7) / 270 * dx;
        MAP(values,25,1,2,3) = static_cast<REAL>(-7) / 270 * dx;
        MAP(values,25,1,2,4) = static_cast<REAL>(-7) / 270 * dx;
        MAP(values,25,1,2,5) = static_cast<REAL>(-7) / 270 * dx;
        MAP(values,25,1,2,6) = static_cast<REAL>(-49) / 270 * dx;
        MAP(values,25,1,2,7) = static_cast<REAL>(-49) / 270 * dx;
        MAP(values,25,2,0,0) = static_cast<REAL>(-7) / 2880 * dy;
        MAP(values,25,2,0,1) = static_cast<REAL>(7) / 2880 * dy;
        MAP(values,25,2,0,2) = static_cast<REAL>(23) / 2880 * dy;
        MAP(values,25,2,0,3) = static_cast<REAL>(-23) / 2880 * dy;
        MAP(values,25,2,0,4) = static_cast<REAL>(-49) / 2880 * dy;
        MAP(values,25,2,0,5) = static_cast<REAL>(49) / 2880 * dy;
        MAP(values,25,2,0,6) = static_cast<REAL>(161) / 2880 * dy;
        MAP(values,25,2,0,7) = static_cast<REAL>(-161) / 2880 * dy;
        MAP(values,25,2,1,0) = static_cast<REAL>(-dx) / 270;
        MAP(values,25,2,1,1) = static_cast<REAL>(-dx) / 270;
        MAP(values,25,2,1,2) = static_cast<REAL>(-7) / 270 * dx;
        MAP(values,25,2,1,3) = static_cast<REAL>(-7) / 270 * dx;
        MAP(values,25,2,1,4) = static_cast<REAL>(-7) / 270 * dx;
        MAP(values,25,2,1,5) = static_cast<REAL>(-7) / 270 * dx;
        MAP(values,25,2,1,6) = static_cast<REAL>(-49) / 270 * dx;
        MAP(values,25,2,1,7) = static_cast<REAL>(-49) / 270 * dx;
        MAP(values,25,2,2,0) = static_cast<REAL>(7) / 1350 * dx * dy / dz;
        MAP(values,25,2,2,1) = static_cast<REAL>(7) / 1350 * dx * dy / dz;
        MAP(values,25,2,2,2) = static_cast<REAL>(-23) / 1350 * dx * dy / dz;
        MAP(values,25,2,2,3) = static_cast<REAL>(-23) / 1350 * dx * dy / dz;
        MAP(values,25,2,2,4) = static_cast<REAL>(49) / 1350 * dx * dy / dz;
        MAP(values,25,2,2,5) = static_cast<REAL>(49) / 1350 * dx * dy / dz;
        MAP(values,25,2,2,6) = static_cast<REAL>(-161) / 1350 * dx * dy / dz;
        MAP(values,25,2,2,7) = static_cast<REAL>(-161) / 1350 * dx * dy / dz;
        MAP(values,26,0,0,0) = static_cast<REAL>(-49) / 86400 / dx * dy * dz;
        MAP(values,26,0,0,1) = static_cast<REAL>(-343) / 86400 / dx * dy * dz;
        MAP(values,26,0,0,2) = static_cast<REAL>(161) / 86400 / dx * dy * dz;
        MAP(values,26,0,0,3) = static_cast<REAL>(1127) / 86400 / dx * dy * dz;
        MAP(values,26,0,0,4) = static_cast<REAL>(161) / 86400 / dx * dy * dz;
        MAP(values,26,0,0,5) = static_cast<REAL>(1127) / 86400 / dx * dy * dz;
        MAP(values,26,0,0,6) = static_cast<REAL>(-529) / 86400 / dx * dy * dz;
        MAP(values,26,0,0,7) = static_cast<REAL>(-3703) / 86400 / dx * dy * dz;
        MAP(values,26,0,1,0) = static_cast<REAL>(7) / 17280 * dz;
        MAP(values,26,0,1,1) = static_cast<REAL>(49) / 17280 * dz;
        MAP(values,26,0,1,2) = static_cast<REAL>(49) / 17280 * dz;
        MAP(values,26,0,1,3) = static_cast<REAL>(343) / 17280 * dz;
        MAP(values,26,0,1,4) = static_cast<REAL>(-23) / 17280 * dz;
        MAP(values,26,0,1,5) = static_cast<REAL>(-161) / 17280 * dz;
        MAP(values,26,0,1,6) = static_cast<REAL>(-161) / 17280 * dz;
        MAP(values,26,0,1,7) = static_cast<REAL>(-1127) / 17280 * dz;
        MAP(values,26,0,2,0) = static_cast<REAL>(7) / 17280 * dy;
        MAP(values,26,0,2,1) = static_cast<REAL>(49) / 17280 * dy;
        MAP(values,26,0,2,2) = static_cast<REAL>(-23) / 17280 * dy;
        MAP(values,26,0,2,3) = static_cast<REAL>(-161) / 17280 * dy;
        MAP(values,26,0,2,4) = static_cast<REAL>(49) / 17280 * dy;
        MAP(values,26,0,2,5) = static_cast<REAL>(343) / 17280 * dy;
        MAP(values,26,0,2,6) = static_cast<REAL>(-161) / 17280 * dy;
        MAP(values,26,0,2,7) = static_cast<REAL>(-1127) / 17280 * dy;
        MAP(values,26,1,0,0) = static_cast<REAL>(7) / 17280 * dz;
        MAP(values,26,1,0,1) = static_cast<REAL>(49) / 17280 * dz;
        MAP(values,26,1,0,2) = static_cast<REAL>(49) / 17280 * dz;
        MAP(values,26,1,0,3) = static_cast<REAL>(343) / 17280 * dz;
        MAP(values,26,1,0,4) = static_cast<REAL>(-23) / 17280 * dz;
        MAP(values,26,1,0,5) = static_cast<REAL>(-161) / 17280 * dz;
        MAP(values,26,1,0,6) = static_cast<REAL>(-161) / 17280 * dz;
        MAP(values,26,1,0,7) = static_cast<REAL>(-1127) / 17280 * dz;
        MAP(values,26,1,1,0) = static_cast<REAL>(-49) / 86400 * dx / dy * dz;
        MAP(values,26,1,1,1) = static_cast<REAL>(161) / 86400 * dx / dy * dz;
        MAP(values,26,1,1,2) = static_cast<REAL>(-343) / 86400 * dx / dy * dz;
        MAP(values,26,1,1,3) = static_cast<REAL>(1127) / 86400 * dx / dy * dz;
        MAP(values,26,1,1,4) = static_cast<REAL>(161) / 86400 * dx / dy * dz;
        MAP(values,26,1,1,5) = static_cast<REAL>(-529) / 86400 * dx / dy * dz;
        MAP(values,26,1,1,6) = static_cast<REAL>(1127) / 86400 * dx / dy * dz;
        MAP(values,26,1,1,7) = static_cast<REAL>(-3703) / 86400 * dx / dy * dz;
        MAP(values,26,1,2,0) = static_cast<REAL>(7) / 17280 * dx;
        MAP(values,26,1,2,1) = static_cast<REAL>(-23) / 17280 * dx;
        MAP(values,26,1,2,2) = static_cast<REAL>(49) / 17280 * dx;
        MAP(values,26,1,2,3) = static_cast<REAL>(-161) / 17280 * dx;
        MAP(values,26,1,2,4) = static_cast<REAL>(49) / 17280 * dx;
        MAP(values,26,1,2,5) = static_cast<REAL>(-161) / 17280 * dx;
        MAP(values,26,1,2,6) = static_cast<REAL>(343) / 17280 * dx;
        MAP(values,26,1,2,7) = static_cast<REAL>(-1127) / 17280 * dx;
        MAP(values,26,2,0,0) = static_cast<REAL>(7) / 17280 * dy;
        MAP(values,26,2,0,1) = static_cast<REAL>(49) / 17280 * dy;
        MAP(values,26,2,0,2) = static_cast<REAL>(-23) / 17280 * dy;
        MAP(values,26,2,0,3) = static_cast<REAL>(-161) / 17280 * dy;
        MAP(values,26,2,0,4) = static_cast<REAL>(49) / 17280 * dy;
        MAP(values,26,2,0,5) = static_cast<REAL>(343) / 17280 * dy;
        MAP(values,26,2,0,6) = static_cast<REAL>(-161) / 17280 * dy;
        MAP(values,26,2,0,7) = static_cast<REAL>(-1127) / 17280 * dy;
        MAP(values,26,2,1,0) = static_cast<REAL>(7) / 17280 * dx;
        MAP(values,26,2,1,1) = static_cast<REAL>(-23) / 17280 * dx;
        MAP(values,26,2,1,2) = static_cast<REAL>(49) / 17280 * dx;
        MAP(values,26,2,1,3) = static_cast<REAL>(-161) / 17280 * dx;
        MAP(values,26,2,1,4) = static_cast<REAL>(49) / 17280 * dx;
        MAP(values,26,2,1,5) = static_cast<REAL>(-161) / 17280 * dx;
        MAP(values,26,2,1,6) = static_cast<REAL>(343) / 17280 * dx;
        MAP(values,26,2,1,7) = static_cast<REAL>(-1127) / 17280 * dx;
        MAP(values,26,2,2,0) = static_cast<REAL>(-49) / 86400 * dx * dy / dz;
        MAP(values,26,2,2,1) = static_cast<REAL>(161) / 86400 * dx * dy / dz;
        MAP(values,26,2,2,2) = static_cast<REAL>(161) / 86400 * dx * dy / dz;
        MAP(values,26,2,2,3) = static_cast<REAL>(-529) / 86400 * dx * dy / dz;
        MAP(values,26,2,2,4) = static_cast<REAL>(-343) / 86400 * dx * dy / dz;
        MAP(values,26,2,2,5) = static_cast<REAL>(1127) / 86400 * dx * dy / dz;
        MAP(values,26,2,2,6) = static_cast<REAL>(1127) / 86400 * dx * dy / dz;
        MAP(values,26,2,2,7) = static_cast<REAL>(-3703) / 86400 * dx * dy / dz;
    }
};
